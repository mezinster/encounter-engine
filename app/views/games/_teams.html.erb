<fieldset>
  <legend>
    <%= t("games.teams.legend") %>
  </legend>

  <p>
    <% teams.each do |team| %>
      <p>
        <strong><%= team.name %></strong>
      </p>
    <% end %>
  </p>
</fieldset>
