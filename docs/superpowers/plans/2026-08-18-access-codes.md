# Access Codes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Let a customer exchange a purchased code for one access pass, and let an operator generate, revoke, expire and inspect those codes.

**Architecture:** A new `access_codes` table stores only a SHA256 digest of each code; the raw code is shown once at generation and never again. Redeeming claims the row with a conditional `UPDATE ... WHERE redeemed_at IS NULL` and mints an `AccessPass` in the same transaction. Revocation and expiry are two nullable columns, appliable to one code or a whole `batch_key`, and they govern only whether a code can still be exchanged — never a run already in progress.

**Tech Stack:** Rails 8.0, Ruby 3.3.12 (rbenv), RSpec, Cucumber (Russian Gherkin, frozen), sqlite in dev/test, `Digest::SHA256`, `SecureRandom`, `ActiveSupport::SecurityUtils`.

**Spec:** `docs/superpowers/specs/2026-08-18-access-codes-design.md`
**Programme umbrella:** `docs/superpowers/specs/2026-08-18-commercial-games-programme-design.md`
**Depends on:** sub-project B (`2026-08-18-access-gated-games-design.md`), already merged into `master`. It shipped `AccessPass`, `AccessPassesController`, `Game#pass_required?` and `User#may_operate_commercial?`.

## Global Constraints

- **Work in the worktree** `/home/mezinster/encounter-engine-access-codes`, branch `feature/access-codes`, which sits directly on `master`. Never commit to `master`. Other sessions hold sibling worktrees.
- **Ruby is not on `PATH` in non-login shells.** Every command assumes you first ran:
  `export PATH="$HOME/.rbenv/bin:$HOME/.rbenv/shims:$PATH"`
- **Never edit any `features/**/*.feature` file**, not even whitespace. They are a byte-identical acceptance contract from the pre-port Merb app. This plan touches none.
- **Stage commits by explicit path**, never `git add -A` — this worktree is shared with other sessions.
- **Hash rockets** (`:key => value`) throughout application code — match the surrounding file.
- **Seven locales, always:** `ru`, `en`, `uk`, `ka`, `tr`, `be`, `pl`. `spec/i18n_spec.rb` enforces exact `ru`↔`en` parity and only a **subset** check for the other five, so a key missing from `uk`/`ka`/`tr`/`be`/`pl` leaves the suite green. `spec/i18n_audit_actions_spec.rb` is different: it scrapes audit-action names out of the controllers and asserts a translation in **every** available locale, so audit labels are hard-covered.
- **Turkish never inflects around a placeholder.** A key carrying `%{team}` or `%{game}` puts the case suffix on a common noun (`«%{team}» adlı takım`).
- **Assert literal Russian in specs, never `I18n.t(key)`** — `ru` is the default locale in test, and `include(I18n.t(key))` cannot fail when a key is missing.
- **`create_user` takes no arguments** and sets the password to `"1234"`. `create_game(options)` accepts `:is_draft => true/false` (translated to `:visibility`) and `:access_mode`. `create_access_pass(:game =>, :team =>)` exists from sub-project B.
- **Do NOT run the full RSpec suite and do NOT run Cucumber from a task.** Run the targeted commands each task names, in the foreground. The controller runs the full-suite gates between tasks, with `bin/rails db:test:prepare` before each.
- **The raw code is never logged** — not via `Rails.logger`, not in an `AdminAction`'s `details`, not in an exception message.
- **`db/schema.rb` is regenerated by running the migration, never hand-edited.**

---

## File Structure

| File | Responsibility | Task |
|---|---|---|
| `db/migrate/20260819100000_create_access_codes.rb` | Create: the table | 1 |
| `app/models/access_code.rb` | Create: normalisation, digest, `redeemable?`, generation | 1 |
| `spec/models/access_code_spec.rb` | Create | 1 |
| `spec/spec_helpers/fixtures_helper.rb` | Modify: `create_access_code` | 1 |
| `app/controllers/access_codes_controller.rb` | Create: operator console — index, create, and the bulk/single state actions | 2, 4, 5 |
| `app/views/access_codes/index.html.erb` | Create: batch list, generation form, lookup box | 2, 4, 5 |
| `app/views/access_codes/created.html.erb` | Create: the one-time export | 2 |
| `app/controllers/access_code_redemptions_controller.rb` | Create: the customer-facing redemption | 3 |
| `app/views/access_code_redemptions/new.html.erb` | Create: the redemption form | 3 |
| `app/models/setting.rb` | Modify: two rate-limit defaults | 3 |
| `config/application.rb:62` | Modify: filter the code parameter | 3 |
| `config/routes.rb` | Modify: the console and redemption routes | 2, 3 |
| `app/views/shared/_current_games_status.html.erb` | Modify: the "access required" line becomes a link | 3 |
| `config/locales/{ru,en,uk,ka,tr,be,pl}.yml` | Modify: keys per task | 1–5 |

**Sequencing follows the spec's §10, and the groups must not interleave** — they fail differently. Task 1 is the code itself, where a defect is permanent for codes already printed. Task 3 is redemption, where a defect gives away runs. Tasks 2, 4 and 5 are the console, where a defect inconveniences an operator who can fall back on the pass console B already ships.

---

## Task 1: `access_codes` and the `AccessCode` model

**Files:**
- Create: `db/migrate/20260819100000_create_access_codes.rb`
- Create: `app/models/access_code.rb`
- Modify: `db/schema.rb` (regenerated), `app/models/game.rb` (association), `spec/spec_helpers/fixtures_helper.rb`
- Modify: `config/locales/{ru,en,uk,ka,tr,be,pl}.yml`
- Test: `spec/models/access_code_spec.rb`

**Interfaces:**
- Consumes: `Game#pass_required?`, `AccessPass` (both from sub-project B, on `master`).
- Produces: `AccessCode::ALPHABET`, `AccessCode.normalize(raw)` → String, `AccessCode.digest(raw)` → String, `AccessCode.generate_batch!(game:, count:, issued_by:, expires_at: nil)` → `[batch_key, [raw_codes]]`, `AccessCode.find_by_code(raw)` → AccessCode or nil, `AccessCode#redeemable?` → Boolean, `AccessCode#state` → Symbol. Tasks 2–5 use all of these.

- [ ] **Step 1: Write the failing test**

Create `spec/models/access_code_spec.rb`:

```ruby
require "rails_helper"

describe AccessCode do
  let(:game) { create_game(:is_draft => false, :access_mode => "pass_required") }

  describe ".normalize" do
    it "upcases" do
      expect(AccessCode.normalize("abcde12345")).to eq("ABCDE12345")
    end

    it "strips spaces and dashes" do
      expect(AccessCode.normalize(" ABCDE-12345 ")).to eq("ABCDE12345")
    end

    # Crockford excludes I, L, O and U from the OUTPUT alphabet precisely so
    # they can be accepted as INPUT aliases. A customer types what they see.
    it "maps the confusables the alphabet excludes" do
      expect(AccessCode.normalize("OIL")).to eq("011")
    end

    it "is nil-safe" do
      expect(AccessCode.normalize(nil)).to eq("")
    end
  end

  describe ".digest" do
    it "digests the normalised form, so a typed O matches a printed 0" do
      expect(AccessCode.digest("o1-234")).to eq(AccessCode.digest("01234"))
    end

    it "differs for different codes" do
      expect(AccessCode.digest("ABCDE12345")).not_to eq(AccessCode.digest("ABCDE12346"))
    end
  end

  describe ".generate_batch!" do
    it "mints the requested number of codes sharing one batch_key" do
      key, raws = AccessCode.generate_batch!(:game => game, :count => 3, :issued_by => create_user)

      expect(raws.length).to eq(3)
      expect(AccessCode.where(:batch_key => key).count).to eq(3)
    end

    it "returns raw codes that are ten characters of the alphabet" do
      _key, raws = AccessCode.generate_batch!(:game => game, :count => 5, :issued_by => create_user)

      raws.each do |raw|
        expect(raw.length).to eq(10)
        expect(raw.chars.all? { |c| AccessCode::ALPHABET.include?(c) }).to be true
      end
    end

    it "stores no raw code anywhere" do
      _key, raws = AccessCode.generate_batch!(:game => game, :count => 1, :issued_by => create_user)

      expect(AccessCode.first.attributes.values.map(&:to_s)).not_to include(raws.first)
    end

    it "mints distinct codes" do
      _key, raws = AccessCode.generate_batch!(:game => game, :count => 20, :issued_by => create_user)

      expect(raws.uniq.length).to eq(20)
    end

    it "carries the batch expiry onto every code" do
      when_ = 3.days.from_now
      _key, _raws = AccessCode.generate_batch!(:game => game, :count => 2,
                                               :issued_by => create_user, :expires_at => when_)

      expect(AccessCode.all.map(&:expires_at).compact.length).to eq(2)
    end
  end

  describe ".find_by_code" do
    it "finds a minted code" do
      _key, raws = AccessCode.generate_batch!(:game => game, :count => 1, :issued_by => create_user)

      expect(AccessCode.find_by_code(raws.first)).to eq(AccessCode.first)
    end

    it "finds it when typed with confusables and a dash" do
      _key, raws = AccessCode.generate_batch!(:game => game, :count => 1, :issued_by => create_user)
      typed = raws.first.dup
      typed[0] = "O" if typed[0] == "0"
      typed = typed.insert(5, "-").downcase

      expect(AccessCode.find_by_code(typed)).to eq(AccessCode.first)
    end

    it "returns nil for an unknown code" do
      AccessCode.generate_batch!(:game => game, :count => 1, :issued_by => create_user)

      expect(AccessCode.find_by_code("ZZZZZZZZZZ")).to be_nil
    end

    # A blank input must never resolve to a row, even one whose digest column
    # is somehow blank -- the same failure User.find_by_reset_token guards.
    it "returns nil for a blank input" do
      expect(AccessCode.find_by_code("")).to be_nil
      expect(AccessCode.find_by_code(nil)).to be_nil
    end
  end

  describe "#redeemable? and #state" do
    def code
      AccessCode.generate_batch!(:game => game, :count => 1, :issued_by => create_user)
      AccessCode.first
    end

    it "is redeemable when fresh" do
      c = code
      expect(c.redeemable?).to be true
      expect(c.state).to eq(:outstanding)
    end

    it "is not redeemable once revoked" do
      c = code
      c.update!(:revoked_at => Time.now)
      expect(c.redeemable?).to be false
      expect(c.state).to eq(:revoked)
    end

    it "is not redeemable once redeemed" do
      c = code
      c.update!(:redeemed_at => Time.now)
      expect(c.redeemable?).to be false
      expect(c.state).to eq(:redeemed)
    end

    it "is not redeemable once expired" do
      c = code
      c.update!(:expires_at => 1.minute.ago)
      expect(c.redeemable?).to be false
      expect(c.state).to eq(:expired)
    end

    it "is redeemable with an expiry still in the future" do
      c = code
      c.update!(:expires_at => 1.day.from_now)
      expect(c.redeemable?).to be true
    end

    # Redemption wins over expiry in the report, because a redeemed code has
    # already produced a pass and the customer's run is not the code's business.
    it "reports :redeemed even if its expiry has since passed" do
      c = code
      c.update!(:redeemed_at => 2.days.ago, :expires_at => 1.day.ago)
      expect(c.state).to eq(:redeemed)
    end
  end

  it "refuses a code on a game that is not gated" do
    scheduled = create_game(:is_draft => false)

    expect {
      AccessCode.generate_batch!(:game => scheduled, :count => 1, :issued_by => create_user)
    }.to raise_error(ActiveRecord::RecordInvalid)
  end
end
```

- [ ] **Step 2: Run it and confirm it fails for the right reason**

Run: `bundle exec rspec spec/models/access_code_spec.rb`
Expected: FAIL — `NameError: uninitialized constant AccessCode`.

- [ ] **Step 3: Write the migration**

Create `db/migrate/20260819100000_create_access_codes.rb`:

```ruby
class CreateAccessCodes < ActiveRecord::Migration[8.0]
  def change
    create_table :access_codes do |t|
      t.integer  :game_id,     :null => false
      # Only the digest is ever stored. The raw code is shown once, at
      # generation, and cannot be recovered from here -- which is why the
      # operator console has a lookup box the customer's own code is typed
      # into. See the design, C3 and C12.
      t.string   :code_digest, :null => false
      # A handle, not a secret: it identifies a print run and grants nothing,
      # so it is safe on screen and in an AdminAction's details.
      t.string   :batch_key,   :null => false
      t.integer  :issued_by_id
      t.datetime :revoked_at
      t.datetime :expires_at
      t.datetime :redeemed_at
      t.integer  :access_pass_id
      t.timestamps
    end

    add_index :access_codes, :code_digest, :unique => true
    add_index :access_codes, [ :game_id, :batch_key ]
  end
end
```

Then `bin/rails db:migrate && bin/rails db:test:prepare`.

- [ ] **Step 4: Write the model**

Create `app/models/access_code.rb`:

```ruby
# -*- encoding : utf-8 -*-
# A secret which, exchanged once, creates exactly one AccessPass.
#
# The code is NOT the entitlement. An unredeemed code belongs to nobody, which
# is what keeps access_passes.team_id NOT NULL -- see the access-gated-games
# design, B4. Redemption is what mints a pass.
class AccessCode < ApplicationRecord
  # Crockford base32: the digits and the uppercase letters, excluding I, L, O
  # and U. Thirty-two symbols, so a ten-character code carries 2^50.
  ALPHABET = "0123456789ABCDEFGHJKMNPQRSTVWXYZ".freeze
  LENGTH   = 10

  belongs_to :game
  belongs_to :issued_by,   :class_name => "User",       :optional => true
  belongs_to :access_pass, :optional => true

  validates :code_digest, :presence => true, :uniqueness => true
  validates :batch_key,   :presence => true
  validate  :game_is_gated, :on => :create

  scope :of_batch, ->(key) { where(:batch_key => key) }

  # One method, used by generation, redemption AND the operator's lookup. If
  # the lookup and the redemption path ever disagreed here, an operator would
  # confirm a code is fine while the customer keeps failing to redeem it.
  #
  # The confusable mapping is the whole point of the alphabet: Crockford
  # excludes I, L, O and U from the OUTPUT so they can be accepted as INPUT
  # aliases for 1, 1, 0 and V. A customer types what they see on the card.
  def self.normalize(raw)
    raw.to_s.upcase.gsub(/[\s\-]/, "").tr("ILOU", "110V")
  end

  def self.digest(raw)
    Digest::SHA256.hexdigest(normalize(raw))
  end

  # Mirrors User.find_by_reset_token's three guards: a blank input resolves to
  # nothing even against a blank stored digest, and the comparison is
  # constant-time rather than trusting the database's indexed equality.
  def self.find_by_code(raw)
    return nil if raw.to_s.strip.empty?

    wanted = digest(raw)
    candidate = where.not(:code_digest => nil).find_by(:code_digest => wanted)
    return nil unless candidate
    return nil unless ActiveSupport::SecurityUtils.secure_compare(candidate.code_digest, wanted)

    candidate
  end

  # Returns [batch_key, raw_codes]. The raw codes are the ONLY copy: they are
  # rendered once by the caller and never persisted.
  #
  # SecureRandom.random_number(ALPHABET.length) per character rather than
  # slicing a longer random string: 32 divides the generator's range exactly,
  # so the draw is uniform. Taking characters modulo an alphabet that does not
  # divide evenly is the standard way to quietly lose entropy.
  def self.generate_batch!(game:, count:, issued_by:, expires_at: nil)
    key  = SecureRandom.hex(6)
    raws = []

    transaction do
      count.times do
        raw = Array.new(LENGTH) { ALPHABET[SecureRandom.random_number(ALPHABET.length)] }.join
        create!(:game => game, :code_digest => digest(raw), :batch_key => key,
                :issued_by => issued_by, :expires_at => expires_at)
        raws << raw
      end
    end

    [ key, raws ]
  end

  def redeemable?
    revoked_at.nil? && redeemed_at.nil? && (expires_at.nil? || expires_at > Time.now)
  end

  # Redeemed wins over expired: a redeemed code has already produced a pass,
  # and that pass's life is not the code's business. See the design, C10.
  def state
    return :redeemed if redeemed_at.present?
    return :revoked  if revoked_at.present?
    return :expired  if expires_at.present? && expires_at <= Time.now

    :outstanding
  end

  private

  def game_is_gated
    return if game&.pass_required?

    errors.add(:game, :not_gated)
  end
end
```

- [ ] **Step 5: Wire the association and the fixture**

In `app/models/game.rb`, beside `has_many :access_passes`:

```ruby
  has_many :access_codes, :dependent => :destroy
```

**`Game#deletable?` must also refuse a game holding codes**, for the same reason it already refuses one holding passes: deleting the game would destroy purchase records. Add `access_codes.empty?` to that conjunction, and add an example asserting a game holding a code is not deletable. Prove it discriminates by removing the conjunct and confirming only that example fails.

In `spec/spec_helpers/fixtures_helper.rb`, beside `create_access_pass`:

```ruby
  # Returns the AccessCode; the raw code is the second element of the pair
  # generate_batch! returns, so a spec that needs to redeem asks for it.
  def create_access_code(options = {})
    game = options[:game] || create_game(:is_draft => false, :access_mode => "pass_required")
    _key, raws = AccessCode.generate_batch!(:game => game, :count => 1,
                                            :issued_by => options[:issued_by] || create_user,
                                            :expires_at => options[:expires_at])
    [ AccessCode.find_by_code(raws.first), raws.first ]
  end
```

- [ ] **Step 6: Add the validation message to all seven locales**

`AccessCode` validates that its game is gated. The message needs **both halves** — `ApplicationHelper#error_messages_for` renders `"%{attribute} %{message}"`, so without the noun the raw English column name is printed. In `ru`, `uk`, `be` and `pl` the predicate agrees in gender with its own noun.

| Locale | `activerecord.attributes.access_code.game` | `activerecord.errors.models.access_code.attributes.game.not_gated` |
|---|---|---|
| `ru` | `Игра` | `не продаётся по доступу` |
| `en` | `Game` | `is not sold by access pass` |
| `uk` | `Гра` | `не продається за доступом` |
| `ka` | `თამაში` | `არ იყიდება წვდომით` |
| `tr` | `Oyun` | `erişim ile satılmıyor` |
| `be` | `Гульня` | `не прадаецца па доступе` |
| `pl` | `Gra` | `nie jest sprzedawana z dostępem` |

- [ ] **Step 7: Run the specs**

Run: `bundle exec rspec spec/models/access_code_spec.rb spec/models/game spec/requests/game_deletion_spec.rb spec/requests/admin_console_spec.rb spec/requests/admin_entries_console_spec.rb spec/i18n_spec.rb`
Expected: 0 failures.

The two admin-console specs are in that list deliberately. `Game#deletable?` is called **once per
row** in the operator's game listings, so adding a conjunct that reads an association turns those
screens into an N+1 unless the association is preloaded — and both specs exist precisely to pin a
flat query count. The identical regression happened one sub-project ago when `access_passes` was
added to the same method. `Admin::GamesController#index` already preloads `game_passings` and
`access_passes`; add `access_codes` beside them, following that precedent.

- [ ] **Step 8: Commit**

```bash
git add db/migrate/20260819100000_create_access_codes.rb db/schema.rb \
        app/models/access_code.rb app/models/game.rb \
        spec/models/access_code_spec.rb spec/spec_helpers/fixtures_helper.rb \
        config/locales/ru.yml config/locales/en.yml config/locales/uk.yml \
        config/locales/ka.yml config/locales/tr.yml config/locales/be.yml \
        config/locales/pl.yml
git commit -m "Add the AccessCode secret

Ten Crockford base32 characters (2^50), stored only as a SHA256 digest
of the normalised form. The raw code exists once, in the array
generate_batch! returns, and is never persisted.

Normalisation is one method used by generation, redemption and the
operator's lookup alike: if those ever disagreed about confusables, an
operator would confirm a code is fine while the customer kept failing
to redeem it. Crockford excludes I, L, O and U from the output alphabet
precisely so they can be accepted as input aliases.

find_by_code mirrors User.find_by_reset_token's guards, including the
constant-time comparison and the refusal to let a blank input resolve
against a blank stored digest.

Game#deletable? now also refuses a game holding codes, for the same
reason it refuses one holding passes.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

## Task 2: generation and the one-time export

**Files:**
- Create: `app/controllers/access_codes_controller.rb`, `app/views/access_codes/index.html.erb`, `app/views/access_codes/created.html.erb`
- Modify: `config/routes.rb` (beside the `access_passes` block at `:264`)
- Modify: `config/locales/{ru,en,uk,ka,tr,be,pl}.yml`
- Test: `spec/requests/access_code_generation_spec.rb`

**Interfaces:**
- Consumes: `AccessCode.generate_batch!(game:, count:, issued_by:, expires_at:)` from Task 1; `User#may_operate_commercial?` and `Game#pass_required?` from sub-project B.
- Produces: routes `game_access_codes_path(game)` (GET index, POST create); the controller's `ensure_commercial_operator` / `ensure_game_is_gated` filters, which Tasks 4 and 5 add actions behind.

- [ ] **Step 1: Write the failing test**

Create `spec/requests/access_code_generation_spec.rb`:

```ruby
require "rails_helper"

describe "generating access codes", type: :request do
  let(:game)     { create_game(:is_draft => false, :access_mode => "pass_required") }
  let(:operator) { u = create_user; u.update!(:is_operator => true); u }
  let(:ordinary) { create_user }

  def sign_in(user)
    put login_path, :params => { :email => user.email, :password => "1234" }
  end

  it "refuses an anonymous visitor" do
    post game_access_codes_path(game), :params => { :count => 3 }
    expect(AccessCode.count).to eq(0)
  end

  it "refuses an ordinary signed-in user" do
    sign_in(ordinary)
    post game_access_codes_path(game), :params => { :count => 3 }
    expect(response).to have_http_status(:unauthorized)
    expect(AccessCode.count).to eq(0)
  end

  it "refuses generation on a scheduled game" do
    scheduled = create_game(:is_draft => false)
    sign_in(operator)
    post game_access_codes_path(scheduled), :params => { :count => 3 }
    expect(AccessCode.count).to eq(0)
  end

  it "mints the requested number for an operator" do
    sign_in(operator)
    expect { post game_access_codes_path(game), :params => { :count => 3 } }
      .to change { AccessCode.count }.by(3)
    expect(AccessCode.pluck(:batch_key).uniq.length).to eq(1)
    expect(AccessCode.pluck(:issued_by_id).uniq).to eq([ operator.id ])
  end

  # The whole point of the screen: the codes exist in the clear exactly once.
  it "renders the raw codes once, and they are real codes" do
    sign_in(operator)
    post game_access_codes_path(game), :params => { :count => 2 }

    expect(response).to have_http_status(:ok)
    # The view renders them grouped, XXXXX-XXXXX, so match that shape --
    # and note find_by_code normalises the dash away, which is exactly what a
    # customer copying the printed form needs it to do.
    shown = response.body.scan(/[0-9A-HJKMNP-TV-Z]{5}-[0-9A-HJKMNP-TV-Z]{5}/).uniq
    expect(shown.length).to be >= 2
    expect(AccessCode.find_by_code(shown.first)).to be_present
  end

  it "warns that this is the only time they are shown" do
    sign_in(operator)
    post game_access_codes_path(game), :params => { :count => 1 }
    expect(response.body).to include("единственный раз")
  end

  it "carries an optional expiry onto the whole batch" do
    sign_in(operator)
    post game_access_codes_path(game), :params => { :count => 2, :expires_at => "2030-01-01" }
    expect(AccessCode.where.not(:expires_at => nil).count).to eq(2)
  end

  it "records an audit entry naming the batch, never a code" do
    sign_in(operator)
    expect { post game_access_codes_path(game), :params => { :count => 2 } }
      .to change { AdminAction.count }.by(1)

    entry = AdminAction.newest_first.first
    expect(entry.action).to eq("generate_access_codes")
    expect(entry.details).to include(AccessCode.first.batch_key)
  end

  it "refuses a count outside the permitted range without minting anything" do
    sign_in(operator)
    expect { post game_access_codes_path(game), :params => { :count => 0 } }
      .not_to change { AccessCode.count }
    expect { post game_access_codes_path(game), :params => { :count => 501 } }
      .not_to change { AccessCode.count }
  end
end
```

- [ ] **Step 2: Run it and confirm it fails for the right reason**

Run: `bundle exec rspec spec/requests/access_code_generation_spec.rb`
Expected: FAIL — `undefined local variable or method 'game_access_codes_path'`.

- [ ] **Step 3: Add the routes**

In `config/routes.rb`, in the block that already nests `access_passes` under `games` (around `:264`):

```ruby
  resources :games, only: [] do
    resources :access_passes, only: [ :index, :create, :destroy ]
    # The secrets that create those passes. A separate screen rather than more
    # sections on the pass console: passes and codes answer different
    # questions and both lists grow.
    resources :access_codes, only: [ :index, :create ]
  end
```

- [ ] **Step 4: Write the controller**

Create `app/controllers/access_codes_controller.rb`. Read `app/controllers/access_passes_controller.rb` first and mirror its filter shape exactly — it solved the same authorization problem one sub-project ago.

```ruby
# -*- encoding : utf-8 -*-
# The operator's code console: mint codes, and (in later tasks) revoke, expire
# and look them up.
#
# Only the digest is stored, so this screen can never display an existing
# code. #create is the one moment the raw values exist, and they are rendered
# straight into the response and dropped.
class AccessCodesController < ApplicationController
  include AdminAudit

  # A batch is a print run, not a bulk import. The ceiling is a guard against
  # a mistyped count minting tens of thousands of rows in one request, not a
  # business rule.
  MAX_PER_BATCH = 500

  before_action :require_authentication!
  before_action :find_game
  before_action :ensure_commercial_operator
  before_action :ensure_game_is_gated

  def index
    @batches = @game.access_codes.order(:created_at)
  end

  def create
    count = params[:count].to_i

    unless count.between?(1, MAX_PER_BATCH)
      redirect_to game_access_codes_path(@game),
                  :alert => t("access_codes.bad_count", :max => MAX_PER_BATCH) and return
    end

    key, @codes = AccessCode.generate_batch!(
      :game => @game, :count => count, :issued_by => current_user,
      :expires_at => params[:expires_at].presence
    )

    # The batch_key, never a code: batch_key is a handle that grants nothing.
    record_admin_action("generate_access_codes", @game, "#{key} (#{count})")
    @batch_key = key
    render :created
  end

  private

  def find_game
    @game = Game.find(params[:game_id])
  end

  def ensure_commercial_operator
    raise Authentication::Unauthorized, t("errors.must_operate_commercial") unless
      current_user.may_operate_commercial?
  end

  def ensure_game_is_gated
    raise Authentication::Unauthorized, t("errors.game_not_gated") unless @game.pass_required?
  end
end
```

`errors.must_operate_commercial` and `errors.game_not_gated` are the two keys sub-project B's review flagged as misleading on the **pass** console — B2 added them. Confirm they exist (`grep -n "must_operate_commercial" config/locales/ru.yml`); if they do not, add them in all seven locales with the same wording B2 used, and say so in your report.

- [ ] **Step 5: Write the two views**

`app/views/access_codes/index.html.erb` — for this task, a heading and the generation form only. Tasks 4 and 5 add the batch list and the lookup box to the same file.

```erb
<h2 class="page-title"><%= t("access_codes.title") %> &mdash; <%= @game.name %></h2>

<%= form_with url: game_access_codes_path(@game), method: :post, local: true do %>
  <p>
    <%= label_tag :count, t("access_codes.generate.count") %>
    <%= number_field_tag :count, 10, :min => 1, :max => AccessCodesController::MAX_PER_BATCH %>
  </p>
  <p>
    <%= label_tag :expires_at, t("access_codes.generate.expires_at") %>
    <%= date_field_tag :expires_at %>
  </p>
  <p><%= submit_tag t("access_codes.generate.submit"), :class => "btn btn--go" %></p>
<% end %>
```

`app/views/access_codes/created.html.erb`:

```erb
<h2 class="page-title"><%= t("access_codes.created.title") %></h2>

<p class="alert"><%= t("access_codes.created.warning") %></p>

<p><%= t("access_codes.created.batch", :key => @batch_key) %></p>

<%# insert would mutate the array element in place; this array is the only
    copy of these secrets that will ever exist, so it is read, not edited. %>
<pre class="codes"><% @codes.each do |raw| %><%= "#{raw[0, 5]}-#{raw[5, 5]}" %>
<% end %></pre>

<p><%= link_to t("access_codes.created.back"), game_access_codes_path(@game) %></p>
```

`<pre>` deliberately: the operator copies these into whatever they use for printing or delivery, and a monospace block one code per line is what survives a paste.

- [ ] **Step 6: Add the keys to all seven locales**

Under a new `access_codes:` block, plus one audit label under `admin.audit.index.action`:

| Key | ru | en |
|---|---|---|
| `access_codes.title` | Коды доступа | Access codes |
| `access_codes.bad_count` | Укажите от 1 до %{max} кодов | Choose between 1 and %{max} codes |
| `access_codes.generate.count` | Сколько кодов | How many codes |
| `access_codes.generate.expires_at` | Действительны до (необязательно) | Valid until (optional) |
| `access_codes.generate.submit` | Создать коды | Generate codes |
| `access_codes.created.title` | Новые коды | New codes |
| `access_codes.created.warning` | Это единственный раз, когда коды показаны. Скопируйте их сейчас — восстановить их будет нельзя. | This is the only time these codes are shown. Copy them now — they cannot be recovered. |
| `access_codes.created.batch` | Партия: %{key} | Batch: %{key} |
| `access_codes.created.back` | К списку кодов | Back to codes |
| `admin.audit.index.action.generate_access_codes` | Создал коды доступа | Generated access codes |

The remaining five locales, same order:

- `uk`: Коди доступу / Вкажіть від 1 до %{max} кодів / Скільки кодів / Дійсні до (необов'язково) / Створити коди / Нові коди / Це єдиний раз, коли коди показані. Скопіюйте їх зараз — відновити їх буде неможливо. / Партія: %{key} / До списку кодів / Створив коди доступу
- `ka`: წვდომის კოდები / მიუთითეთ 1-დან %{max}-მდე კოდი / რამდენი კოდი / ძალაშია (არასავალდებულო) / კოდების შექმნა / ახალი კოდები / კოდები მხოლოდ ერთხელ ჩანს. დააკოპირეთ ახლავე — აღდგენა შეუძლებელია. / პარტია: %{key} / კოდების სიაში / შექმნა წვდომის კოდები
- `tr`: Erişim kodları / 1 ile %{max} arasında kod belirtin / Kaç kod / Geçerlilik tarihi (isteğe bağlı) / Kod oluştur / Yeni kodlar / Kodlar yalnızca bir kez gösterilir. Şimdi kopyalayın — geri alınamaz. / Parti: %{key} / Kod listesine / Erişim kodları oluşturdu
- `be`: Коды доступу / Пазначце ад 1 да %{max} кодаў / Колькі кодаў / Дзейнічаюць да (неабавязкова) / Стварыць коды / Новыя коды / Гэта адзіны раз, калі коды паказаны. Скапіруйце іх зараз — аднавіць іх будзе немагчыма. / Партыя: %{key} / Да спісу кодаў / Стварыў коды доступу
- `pl`: Kody dostępu / Podaj od 1 do %{max} kodów / Ile kodów / Ważne do (opcjonalnie) / Utwórz kody / Nowe kody / To jedyny raz, gdy kody są pokazane. Skopiuj je teraz — nie da się ich odzyskać. / Partia: %{key} / Do listy kodów / Utworzył kody dostępu

None of these interpolates a user-supplied name, so the Turkish suffix rule does not bite here.

- [ ] **Step 7: Run the specs**

Run: `bundle exec rspec spec/requests/access_code_generation_spec.rb spec/requests/admin_audit_spec.rb spec/i18n_spec.rb spec/i18n_audit_actions_spec.rb`
Expected: 0 failures. `i18n_audit_actions_spec.rb` scrapes action names out of the controllers and asserts a translation in **every** available locale, so it fails loudly if any of the seven is missing the new audit label.

- [ ] **Step 8: Commit**

```bash
git add app/controllers/access_codes_controller.rb app/views/access_codes \
        config/routes.rb spec/requests/access_code_generation_spec.rb \
        config/locales/ru.yml config/locales/en.yml config/locales/uk.yml \
        config/locales/ka.yml config/locales/tr.yml config/locales/be.yml \
        config/locales/pl.yml
git commit -m "Let an operator generate a batch of access codes

The raw codes exist in exactly one place: the response to the POST that
minted them. They are rendered monospace, one per line, with a plain
statement that this is the only time they will be shown, and nothing
persists them.

The audit entry names the batch_key and the count, never a code --
batch_key is a handle that grants nothing.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

## Task 3: redemption

**Files:**
- Create: `app/controllers/access_code_redemptions_controller.rb`, `app/views/access_code_redemptions/new.html.erb`
- Modify: `config/routes.rb`, `app/models/setting.rb` (`RATE_LIMIT_DEFAULTS`, `:11`), `config/application.rb:62`, `app/views/shared/_current_games_status.html.erb`
- Modify: `config/locales/{ru,en,uk,ka,tr,be,pl}.yml`
- Test: `spec/requests/access_code_redemption_spec.rb`

**Interfaces:**
- Consumes: `AccessCode.find_by_code`, `#redeemable?`, `#state` (Task 1); `AccessPass` and `Game#pass_required?` (sub-project B).
- Produces: `redeem_access_code_path` (GET form, POST). Nothing later in this plan depends on it.

**This is the task where a defect gives away runs.** Read the spec's C7 before starting.

- [ ] **Step 1: Write the failing test**

Create `spec/requests/access_code_redemption_spec.rb`:

```ruby
require "rails_helper"

describe "redeeming an access code", type: :request do
  let(:game)    { create_game(:is_draft => false, :access_mode => "pass_required") }
  let(:captain) { create_user }
  let(:team)    { create_team(:captain => captain) }

  def sign_in(user)
    put login_path, :params => { :email => user.email, :password => "1234" }
  end

  def fresh_code
    _code, raw = create_access_code(:game => game)
    raw
  end

  it "creates a pass for the captain's team" do
    raw = fresh_code
    team
    sign_in(captain)

    expect { post redeem_access_code_path, :params => { :access_code => raw } }
      .to change { AccessPass.count }.by(1)

    pass = AccessPass.last
    expect(pass.team_id).to eq(team.id)
    expect(pass.game_id).to eq(game.id)
    expect(pass.source).to eq("access_code")
  end

  it "records what the code became" do
    raw = fresh_code
    team
    sign_in(captain)

    post redeem_access_code_path, :params => { :access_code => raw }

    code = AccessCode.find_by_code(raw)
    expect(code.redeemed_at).to be_present
    expect(code.access_pass_id).to eq(AccessPass.last.id)
  end

  it "accepts a code typed lowercase, with a dash, using confusables" do
    raw = fresh_code
    team
    sign_in(captain)
    typed = raw.dup.insert(5, "-").downcase.tr("01", "oi")

    expect { post redeem_access_code_path, :params => { :access_code => typed } }
      .to change { AccessPass.count }.by(1)
  end

  # C5: the code binds a purchase to one team, so the person who speaks for
  # the team is the one who may spend it.
  it "refuses a team member who is not the captain" do
    raw = fresh_code
    member = create_user
    team.members << member
    sign_in(member)

    expect { post redeem_access_code_path, :params => { :access_code => raw } }
      .not_to change { AccessPass.count }
  end

  it "refuses an anonymous visitor" do
    raw = fresh_code
    expect { post redeem_access_code_path, :params => { :access_code => raw } }
      .not_to change { AccessPass.count }
  end

  # Spec §5: a customer may well buy a code before forming a team. Sending
  # them to a 401 would be a dead end; they are sent to create one, and the
  # code is kept so they do not have to retype it off the card.
  it "sends a teamless user to create a team, keeping the code" do
    raw = fresh_code
    loner = create_user
    sign_in(loner)

    post redeem_access_code_path, :params => { :access_code => raw }

    expect(response).to redirect_to(new_team_path)
    expect(AccessPass.count).to eq(0)

    # And the form pre-fills it once they have a team.
    team = create_team(:captain => loner)
    get redeem_access_code_path
    expect(response.body).to include(raw)
  end

  describe "refusals, each with its own message" do
    before { team; sign_in(captain) }

    it "reports an unknown code" do
      post redeem_access_code_path, :params => { :access_code => "ZZZZZZZZZZ" }
      expect(AccessPass.count).to eq(0)
      expect(flash[:alert]).to include("не найден")
    end

    it "reports an already-redeemed code" do
      raw = fresh_code
      AccessCode.find_by_code(raw).update!(:redeemed_at => Time.now)

      post redeem_access_code_path, :params => { :access_code => raw }
      expect(AccessPass.count).to eq(0)
      expect(flash[:alert]).to include("уже использован")
    end

    it "reports a revoked code" do
      raw = fresh_code
      AccessCode.find_by_code(raw).update!(:revoked_at => Time.now)

      post redeem_access_code_path, :params => { :access_code => raw }
      expect(AccessPass.count).to eq(0)
      expect(flash[:alert]).to include("отозван")
    end

    it "reports an expired code" do
      raw = fresh_code
      AccessCode.find_by_code(raw).update!(:expires_at => 1.minute.ago)

      post redeem_access_code_path, :params => { :access_code => raw }
      expect(AccessPass.count).to eq(0)
      expect(flash[:alert]).to include("истёк")
    end

    it "reports a game that is no longer available" do
      raw = fresh_code
      game.withdraw!

      post redeem_access_code_path, :params => { :access_code => raw }
      expect(AccessPass.count).to eq(0)
      expect(flash[:alert]).to include("недоступна")
    end
  end

  # C11: B6 lets a team hold several passes, consumed oldest-first.
  it "allows a second redemption while the team already holds a live pass" do
    first = fresh_code
    second = fresh_code
    team
    sign_in(captain)

    post redeem_access_code_path, :params => { :access_code => first }
    expect { post redeem_access_code_path, :params => { :access_code => second } }
      .to change { AccessPass.count }.by(1)
  end

  # C7: the claim is a conditional UPDATE, not a Ruby-side nil check. Exercise
  # the claim itself rather than simulating threads: a second claim against an
  # already-claimed row must affect zero rows and create nothing.
  it "cannot be claimed twice" do
    raw = fresh_code
    team
    sign_in(captain)
    post redeem_access_code_path, :params => { :access_code => raw }

    expect { post redeem_access_code_path, :params => { :access_code => raw } }
      .not_to change { AccessPass.count }
  end
end
```

- [ ] **Step 2: Run it and confirm it fails for the right reason**

Run: `bundle exec rspec spec/requests/access_code_redemption_spec.rb`
Expected: FAIL — `undefined local variable or method 'redeem_access_code_path'`.

- [ ] **Step 3: Add the route, the throttle settings and the parameter filter**

`config/routes.rb`, outside the `games` block:

```ruby
  # One global form: the code carries its own game, so a customer holding a
  # card need not find the game first.
  get  "/redeem", to: "access_code_redemptions#new",    as: :redeem_access_code
  post "/redeem", to: "access_code_redemptions#create"
```

`app/models/setting.rb`, in `RATE_LIMIT_DEFAULTS` (`:11`):

```ruby
    "access_code_redemption_max"            => 10,
    "access_code_redemption_window_seconds" => 3600
```

`config/application.rb:62` — the raw code must never reach a log:

```ruby
    config.filter_parameters += [:password, :password_confirmation, :secret, :token, :access_code]
```

- [ ] **Step 4: Write the controller**

Create `app/controllers/access_code_redemptions_controller.rb`:

```ruby
# -*- encoding : utf-8 -*-
# A customer exchanging a purchased code for one access pass.
#
# NOT an administrative act: no AdminAction is written here. The operator
# console shows redemptions through access_codes.access_pass_id instead.
class AccessCodeRedemptionsController < ApplicationController
  include SecurityFilters
  include RequestThrottling

  before_action :require_authentication!
  before_action :send_teamless_user_to_create_one, :only => [ :create ]
  before_action :ensure_team_captain, :only => [ :create ]

  def new
    @access_code = session.delete(:pending_access_code)
  end

  def create
    unless throttle!(:access_code_redemption)
      redirect_to redeem_access_code_path, :alert => t("errors.access_code.throttled") and return
    end

    code = AccessCode.find_by_code(params[:access_code])
    return refuse(:unknown) if code.nil?
    return refuse(code.state) unless code.redeemable?
    return refuse(:game_unavailable) unless playable?(code.game)

    pass = claim(code)
    return refuse(:redeemed) if pass.nil?

    redirect_to show_current_level_path(:game_id => code.game_id),
                :notice => t("access_code_redemptions.redeemed")
  end

  private

  # A customer may buy a code before forming a team, and a 401 would be a dead
  # end. Runs BEFORE ensure_team_captain, so a user with no team at all gets
  # this redirect rather than the captain refusal -- a user who has a team but
  # does not speak for it still gets the 401, which is the point of C5.
  #
  # The code rides in the session, not the URL: it is a secret, and a query
  # string reaches logs, history and every proxy between.
  def send_teamless_user_to_create_one
    return if current_user.team

    session[:pending_access_code] = params[:access_code]
    redirect_to new_team_path, :alert => t("access_code_redemptions.need_a_team")
  end

  # THE conditional claim. The unique index on code_digest stops two codes
  # sharing a digest; it does NOT stop one code being redeemed twice, because
  # two requests can both read redeemed_at IS NULL and both mint a pass. That
  # failure is silent and costs a free run, so the claim is an UPDATE whose
  # WHERE carries the precondition, and we proceed only if it took the row.
  #
  # Returns the pass, or nil when another request won the race.
  def claim(code)
    AccessCode.transaction do
      pass = AccessPass.create!(:game => code.game, :team => current_user.team,
                                :source => "access_code")

      taken = AccessCode.where(:id => code.id, :redeemed_at => nil)
                        .update_all(:redeemed_at => Time.now, :access_pass_id => pass.id,
                                    :updated_at => Time.now)

      raise ActiveRecord::Rollback if taken.zero?

      return pass
    end

    nil
  end

  # A code cannot outlive its game's availability: withdrawn, unpublished, or
  # no longer sold by pass.
  def playable?(game)
    game.pass_required? && game.listed? && !game.withdrawn?
  end

  def refuse(reason)
    redirect_to redeem_access_code_path, :alert => t("errors.access_code.#{reason}")
  end
end
```

**`AccessPass::SOURCES` currently contains only `"operator_invite"`.** Add `"access_code"` to it in `app/models/access_pass.rb`, or `create!` here raises on the inclusion validation. Sub-project B's model comment already anticipates this.

- [ ] **Step 5: Write the form and link it from the catalogue**

`app/views/access_code_redemptions/new.html.erb`:

```erb
<h2 class="page-title"><%= t("access_code_redemptions.title") %></h2>

<%= form_with url: redeem_access_code_path, method: :post, local: true do %>
  <p>
    <%= label_tag :access_code, t("access_code_redemptions.code") %>
    <%= text_field_tag :access_code, @access_code, :autocomplete => "off", :autocapitalize => "characters" %>
  </p>
  <p><%= submit_tag t("access_code_redemptions.submit"), :class => "btn btn--go" %></p>
<% end %>
```

In `app/views/shared/_current_games_status.html.erb`, sub-project B2 renders a bare "access required" line in the `pass_required?` branch when the team has no live pass. Turn that line into a link to `redeem_access_code_path`, keeping the same key for the text. Read the partial's comment first — it explains why `gated_live` is passed in rather than computed per row, and that reasoning is unchanged.

- [ ] **Step 6: Add the keys to all seven locales**

| Key | ru | en |
|---|---|---|
| `access_code_redemptions.title` | Ввести код доступа | Enter an access code |
| `access_code_redemptions.code` | Код | Code |
| `access_code_redemptions.submit` | Активировать | Redeem |
| `access_code_redemptions.redeemed` | Код принят. Игра открыта для вашей команды. | Code accepted. The game is open to your team. |
| `errors.access_code.unknown` | Такой код не найден | No such code |
| `errors.access_code.redeemed` | Этот код уже использован | This code has already been used |
| `errors.access_code.revoked` | Этот код отозван | This code has been revoked |
| `errors.access_code.expired` | Срок действия кода истёк | This code has expired |
| `errors.access_code.game_unavailable` | Игра сейчас недоступна | That game is not available |
| `errors.access_code.throttled` | Слишком много попыток. Попробуйте позже. | Too many attempts. Try again later. |
| `access_code_redemptions.need_a_team` | Сначала создайте команду — код мы сохранили. | Create a team first — we have kept your code. |

The remaining five, same order:

- `uk`: Ввести код доступу / Код / Активувати / Код прийнято. Гра відкрита для вашої команди. / Такий код не знайдено / Цей код уже використано / Цей код відкликано / Термін дії коду минув / Гра зараз недоступна / Забагато спроб. Спробуйте пізніше. / Спочатку створіть команду — код ми зберегли.
- `ka`: შეიყვანეთ წვდომის კოდი / კოდი / გააქტიურება / კოდი მიღებულია. თამაში ხელმისაწვდომია თქვენი გუნდისთვის. / ასეთი კოდი ვერ მოიძებნა / ეს კოდი უკვე გამოყენებულია / ეს კოდი ჩამორთმეულია / კოდის ვადა გავიდა / თამაში ამჟამად მიუწვდომელია / ძალიან ბევრი მცდელობა. სცადეთ მოგვიანებით. / ჯერ შექმენით გუნდი — კოდი შევინახეთ.
- `tr`: Erişim kodu girin / Kod / Kullan / Kod kabul edildi. Oyun takımınıza açıldı. / Böyle bir kod bulunamadı / Bu kod zaten kullanılmış / Bu kod geri alınmış / Kodun süresi dolmuş / Oyun şu anda kullanılamıyor / Çok fazla deneme. Daha sonra tekrar deneyin. / Önce bir takım oluşturun — kodunuzu sakladık.
- `be`: Увесці код доступу / Код / Актываваць / Код прыняты. Гульня адкрыта для вашай каманды. / Такі код не знойдзены / Гэты код ужо выкарыстаны / Гэты код адкліканы / Тэрмін дзеяння кода скончыўся / Гульня зараз недаступная / Занадта шмат спроб. Паспрабуйце пазней. / Спачатку стварыце каманду — код мы захавалі.
- `pl`: Wprowadź kod dostępu / Kod / Aktywuj / Kod przyjęty. Gra jest otwarta dla twojej drużyny. / Nie znaleziono takiego kodu / Ten kod został już użyty / Ten kod został odebrany / Kod wygasł / Gra jest obecnie niedostępna / Zbyt wiele prób. Spróbuj później. / Najpierw utwórz drużynę — zachowaliśmy twój kod.

The Russian strings are what the spec's refusal examples assert on, so they must match exactly: «не найден», «уже использован», «отозван», «истёк», «недоступна».

- [ ] **Step 7: Run the specs**

Run: `bundle exec rspec spec/requests/access_code_redemption_spec.rb spec/models/access_pass spec/requests/gated_play_spec.rb spec/requests/games_listing_spec.rb spec/i18n_spec.rb`
Expected: 0 failures. `games_listing_spec.rb` is included because Step 5 edits a partial that renders once per row in the games list and pins a flat query count.

- [ ] **Step 8: Commit**

```bash
git add app/controllers/access_code_redemptions_controller.rb \
        app/views/access_code_redemptions app/models/access_pass.rb \
        app/views/shared/_current_games_status.html.erb \
        app/models/setting.rb config/application.rb config/routes.rb \
        spec/requests/access_code_redemption_spec.rb \
        config/locales/ru.yml config/locales/en.yml config/locales/uk.yml \
        config/locales/ka.yml config/locales/tr.yml config/locales/be.yml \
        config/locales/pl.yml
git commit -m "Let a captain redeem an access code for a pass

The claim is a conditional UPDATE whose WHERE carries the precondition
-- redeemed_at IS NULL -- checked by affected-row count inside the
transaction that mints the pass. The unique index on the digest does
not prevent a double redemption: two requests can both read the row as
unclaimed and both mint a pass, silently giving away a run and leaving
two passes pointing at one purchase.

Refusals are specific, which the code's 2^50 of entropy is what makes
safe: distinguishing 'unknown' from 'already used' leaks whether a
string is a real code, and an attacker cannot produce a candidate to
test.

The raw code is added to filter_parameters so it never reaches a log.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

## Task 4: revocation and expiry

**Files:**
- Modify: `app/controllers/access_codes_controller.rb`, `config/routes.rb`
- Modify: `config/locales/{ru,en,uk,ka,tr,be,pl}.yml`
- Test: `spec/requests/access_code_state_spec.rb`

**Interfaces:**
- Consumes: `AccessCode#state`, `#redeemable?`, `.of_batch(key)` (Task 1); the controller and its filters (Task 2).
- Produces: `revoke_game_access_codes_path`, `unrevoke_game_access_codes_path`, `expiry_game_access_codes_path` — all collection PATCHes taking **either** `code_id` **or** `batch_key`. Task 5's list renders buttons for them.

**Three actions, not six.** Each takes either a single `code_id` or a `batch_key`, resolved by one private method. That is what keeps "revoke this code" and "revoke this batch" from drifting into two implementations of one rule, which is the failure this programme has already paid for once (three resolutions of "the team's gated attempt", found by sub-project B's whole-branch review).

- [ ] **Step 1: Write the failing test**

Create `spec/requests/access_code_state_spec.rb`:

```ruby
require "rails_helper"

describe "revoking and expiring access codes", type: :request do
  let(:game)     { create_game(:is_draft => false, :access_mode => "pass_required") }
  let(:operator) { u = create_user; u.update!(:is_operator => true); u }

  def sign_in(user)
    put login_path, :params => { :email => user.email, :password => "1234" }
  end

  def batch_of(n)
    key, raws = AccessCode.generate_batch!(:game => game, :count => n, :issued_by => operator)
    [ key, raws ]
  end

  it "refuses an ordinary signed-in user" do
    key, _ = batch_of(1)
    sign_in(create_user)

    patch revoke_game_access_codes_path(game), :params => { :batch_key => key }

    expect(response).to have_http_status(:unauthorized)
    expect(AccessCode.first.revoked_at).to be_nil
  end

  it "revokes a single code" do
    _key, raws = batch_of(2)
    code = AccessCode.find_by_code(raws.first)
    sign_in(operator)

    patch revoke_game_access_codes_path(game), :params => { :code_id => code.id }

    expect(code.reload.revoked_at).to be_present
    expect(AccessCode.where(:revoked_at => nil).count).to eq(1)
  end

  it "revokes a whole batch" do
    key, _ = batch_of(3)
    sign_in(operator)

    patch revoke_game_access_codes_path(game), :params => { :batch_key => key }

    expect(AccessCode.where.not(:revoked_at => nil).count).to eq(3)
  end

  it "lifts a revocation" do
    key, _ = batch_of(2)
    sign_in(operator)
    patch revoke_game_access_codes_path(game), :params => { :batch_key => key }

    patch unrevoke_game_access_codes_path(game), :params => { :batch_key => key }

    expect(AccessCode.where(:revoked_at => nil).count).to eq(2)
  end

  it "sets an expiry on a batch" do
    key, _ = batch_of(2)
    sign_in(operator)

    patch expiry_game_access_codes_path(game), :params => { :batch_key => key, :expires_at => "2030-01-01" }

    expect(AccessCode.where.not(:expires_at => nil).count).to eq(2)
  end

  it "clears an expiry when given a blank date" do
    key, _ = batch_of(2)
    sign_in(operator)
    patch expiry_game_access_codes_path(game), :params => { :batch_key => key, :expires_at => "2030-01-01" }

    patch expiry_game_access_codes_path(game), :params => { :batch_key => key, :expires_at => "" }

    expect(AccessCode.where(:expires_at => nil).count).to eq(2)
  end

  # C10: these columns govern whether a code can still be EXCHANGED. A
  # redeemed code has already produced a pass, and that pass's life is not the
  # code's business -- expiring it must not reach through and end a run.
  it "leaves a redeemed code's pass untouched when the batch is revoked" do
    key, raws = batch_of(2)
    code = AccessCode.find_by_code(raws.first)
    pass = create_access_pass(:game => game)
    code.update!(:redeemed_at => Time.now, :access_pass_id => pass.id)
    sign_in(operator)

    patch revoke_game_access_codes_path(game), :params => { :batch_key => key }

    expect(pass.reload.revoked_at).to be_nil
    expect(code.reload.redeemed_at).to be_present
    expect(code.state).to eq(:redeemed)
  end

  it "cannot reach another game's codes" do
    other = create_game(:is_draft => false, :access_mode => "pass_required")
    key, _ = AccessCode.generate_batch!(:game => other, :count => 1, :issued_by => operator)
    sign_in(operator)

    patch revoke_game_access_codes_path(game), :params => { :batch_key => key }

    expect(AccessCode.first.revoked_at).to be_nil
  end

  it "records an audit entry naming the batch" do
    key, _ = batch_of(2)
    sign_in(operator)

    expect { patch revoke_game_access_codes_path(game), :params => { :batch_key => key } }
      .to change { AdminAction.count }.by(1)
    expect(AdminAction.newest_first.first.action).to eq("revoke_access_codes")
  end
end
```

- [ ] **Step 2: Run it and confirm it fails for the right reason**

Run: `bundle exec rspec spec/requests/access_code_state_spec.rb`
Expected: FAIL — `undefined local variable or method 'revoke_game_access_codes_path'`.

- [ ] **Step 3: Add the routes**

In `config/routes.rb`, inside the `resources :access_codes` block:

```ruby
    resources :access_codes, only: [ :index, :create ] do
      collection do
        # Each takes EITHER code_id OR batch_key -- one rule, two scopes. See
        # the controller's targeted_codes.
        patch :revoke
        patch :unrevoke
        patch :expiry
      end
    end
```

- [ ] **Step 4: Add the three actions and the one resolver**

In `app/controllers/access_codes_controller.rb`, after `#create`:

```ruby
  # Revocation and expiry govern whether a code can still be EXCHANGED, and
  # nothing else. Neither reaches a code that has already been redeemed: that
  # code has produced an AccessPass, and ending a run the customer paid for is
  # a separate, deliberate act through the pass. See the design, C10.
  def revoke
    n = targeted_codes.update_all(:revoked_at => Time.now, :updated_at => Time.now)
    record_admin_action("revoke_access_codes", @game, target_label(n))
    redirect_to game_access_codes_path(@game), :notice => t("access_codes.revoked_notice", :count => n)
  end

  def unrevoke
    n = targeted_codes.update_all(:revoked_at => nil, :updated_at => Time.now)
    record_admin_action("unrevoke_access_codes", @game, target_label(n))
    redirect_to game_access_codes_path(@game), :notice => t("access_codes.unrevoked_notice", :count => n)
  end

  def expiry
    when_ = params[:expires_at].presence
    n = targeted_codes.update_all(:expires_at => when_, :updated_at => Time.now)
    record_admin_action("set_access_code_expiry", @game, "#{target_label(n)} -> #{when_ || "-"}")
    redirect_to game_access_codes_path(@game), :notice => t("access_codes.expiry_notice", :count => n)
  end

  private

  # ONE definition of "which codes is this operator acting on", used by all
  # three actions. Always scoped through @game, so a batch_key or id belonging
  # to another game resolves to nothing rather than to somebody else's codes.
  def targeted_codes
    scope = @game.access_codes
    return scope.where(:id => params[:code_id]) if params[:code_id].present?

    scope.of_batch(params[:batch_key].to_s)
  end

  # The batch key or the code's id -- never the code itself, which this
  # application does not hold and must never write to an audit entry.
  def target_label(count)
    params[:code_id].present? ? "code ##{params[:code_id]} (#{count})" : "#{params[:batch_key]} (#{count})"
  end
```

Note `update_all` bypasses validations and callbacks deliberately: these are mechanical bulk state changes on rows whose other columns are not being touched, the same reasoning `Game#pause!` records for `update_column`.

- [ ] **Step 5: Add the keys to all seven locales**

| Key | ru | en |
|---|---|---|
| `access_codes.revoked_notice` | Отозвано кодов: %{count} | Codes revoked: %{count} |
| `access_codes.unrevoked_notice` | Возвращено кодов: %{count} | Codes restored: %{count} |
| `access_codes.expiry_notice` | Изменён срок у кодов: %{count} | Expiry changed on %{count} codes |
| `admin.audit.index.action.revoke_access_codes` | Отозвал коды доступа | Revoked access codes |
| `admin.audit.index.action.unrevoke_access_codes` | Вернул коды доступа | Restored access codes |
| `admin.audit.index.action.set_access_code_expiry` | Изменил срок действия кодов | Changed access-code expiry |

The remaining five, same order:

- `uk`: Відкликано кодів: %{count} / Повернено кодів: %{count} / Змінено термін у кодів: %{count} / Відкликав коди доступу / Повернув коди доступу / Змінив термін дії кодів
- `ka`: ჩამორთმეულია კოდი: %{count} / დაბრუნებულია კოდი: %{count} / შეიცვალა ვადა კოდებზე: %{count} / ჩამოართვა წვდომის კოდები / დააბრუნა წვდომის კოდები / შეცვალა კოდების ვადა
- `tr`: Geri alınan kod: %{count} / Geri verilen kod: %{count} / Süresi değişen kod: %{count} / Erişim kodlarını geri aldı / Erişim kodlarını geri verdi / Erişim kodu süresini değiştirdi
- `be`: Адклікана кодаў: %{count} / Вернута кодаў: %{count} / Зменены тэрмін у кодаў: %{count} / Адклікаў коды доступу / Вярнуў коды доступу / Змяніў тэрмін дзеяння кодаў
- `pl`: Odebrano kodów: %{count} / Przywrócono kodów: %{count} / Zmieniono termin dla kodów: %{count} / Odebrał kody dostępu / Przywrócił kody dostępu / Zmienił termin ważności kodów

`%{count}` here is **plain interpolation, not I18n pluralisation** — this app has no pluralised keys and `rails-i18n` supplies the CLDR rules only if one is written. Keep it a bare `%{count}`, matching `"Коды (%{count}):"` elsewhere in these files.

- [ ] **Step 6: Run the specs**

Run: `bundle exec rspec spec/requests/access_code_state_spec.rb spec/requests/admin_audit_spec.rb spec/i18n_spec.rb spec/i18n_audit_actions_spec.rb`
Expected: 0 failures.

- [ ] **Step 7: Commit**

```bash
git add app/controllers/access_codes_controller.rb config/routes.rb \
        spec/requests/access_code_state_spec.rb \
        config/locales/ru.yml config/locales/en.yml config/locales/uk.yml \
        config/locales/ka.yml config/locales/tr.yml config/locales/be.yml \
        config/locales/pl.yml
git commit -m "Let an operator revoke and expire codes, singly or by batch

Three actions, not six: each takes either a code_id or a batch_key,
resolved by one private method scoped through the game. One rule, two
scopes -- this programme has already paid once for the same concept
growing three implementations.

Neither revocation nor expiry reaches a redeemed code. Those columns
govern whether a code can still be exchanged; ending a run the customer
already paid for is a separate act through the pass.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

## Task 5: the batch list and the lookup box

**Files:**
- Modify: `app/controllers/access_codes_controller.rb` (`#index`, plus `#lookup`), `app/views/access_codes/index.html.erb`, `config/routes.rb`
- Modify: `config/locales/{ru,en,uk,ka,tr,be,pl}.yml`
- Test: `spec/requests/access_code_console_spec.rb`

**Interfaces:**
- Consumes: everything from Tasks 1, 2 and 4.
- Produces: nothing later depends on it. This is the last task.

**The lookup is a POST, not a GET.** A GET would put the customer's secret in a query string, which lands in server logs, browser history and any proxy in between — the one place `filter_parameters` cannot reach.

- [ ] **Step 1: Write the failing test**

Create `spec/requests/access_code_console_spec.rb`:

```ruby
require "rails_helper"

describe "the access-code console", type: :request do
  let(:game)     { create_game(:is_draft => false, :access_mode => "pass_required") }
  let(:operator) { u = create_user; u.update!(:is_operator => true); u }

  def sign_in(user)
    put login_path, :params => { :email => user.email, :password => "1234" }
  end

  it "refuses an ordinary signed-in user" do
    sign_in(create_user)
    get game_access_codes_path(game)
    expect(response).to have_http_status(:unauthorized)
  end

  it "lists a batch with its counts and never shows a code" do
    key, raws = AccessCode.generate_batch!(:game => game, :count => 3, :issued_by => operator)
    AccessCode.find_by_code(raws.first).update!(:revoked_at => Time.now)
    sign_in(operator)

    get game_access_codes_path(game)

    expect(response.body).to include(key)
    expect(response.body).not_to include(raws.first)
    expect(response.body).not_to include(raws.last)
  end

  # The whole reason the lookup exists: digest-only storage leaves no other
  # way for an operator to answer "my code does not work".
  it "resolves a code the operator cannot see" do
    _key, raws = AccessCode.generate_batch!(:game => game, :count => 1, :issued_by => operator)
    sign_in(operator)

    post lookup_game_access_codes_path(game), :params => { :access_code => raws.first }

    expect(response).to have_http_status(:ok)
    expect(response.body).to include(AccessCode.first.batch_key)
  end

  it "resolves a code typed with confusables and a dash" do
    _key, raws = AccessCode.generate_batch!(:game => game, :count => 1, :issued_by => operator)
    typed = raws.first.dup.insert(5, "-").downcase.tr("01", "oi")
    sign_in(operator)

    post lookup_game_access_codes_path(game), :params => { :access_code => typed }

    expect(response.body).to include(AccessCode.first.batch_key)
  end

  it "reports an unknown code without raising" do
    sign_in(operator)

    post lookup_game_access_codes_path(game), :params => { :access_code => "ZZZZZZZZZZ" }

    expect(response).to have_http_status(:ok)
    expect(response.body).to include("не найден")
  end

  it "does not resolve another game's code" do
    other = create_game(:is_draft => false, :access_mode => "pass_required")
    _key, raws = AccessCode.generate_batch!(:game => other, :count => 1, :issued_by => operator)
    sign_in(operator)

    post lookup_game_access_codes_path(game), :params => { :access_code => raws.first }

    expect(response.body).to include("не найден")
  end

  it "shows which team a redeemed code became" do
    _key, raws = AccessCode.generate_batch!(:game => game, :count => 1, :issued_by => operator)
    pass = create_access_pass(:game => game)
    AccessCode.first.update!(:redeemed_at => Time.now, :access_pass_id => pass.id)
    sign_in(operator)

    post lookup_game_access_codes_path(game), :params => { :access_code => raws.first }

    expect(response.body).to include(pass.team.name)
  end

  # Sub-project B broke two query-count specs by adding a per-row read behind
  # a listing. The batch summary must be ONE grouped query, not one per batch.
  it "keeps the query count flat as the number of batches grows" do
    sign_in(operator)
    2.times { AccessCode.generate_batch!(:game => game, :count => 2, :issued_by => operator) }
    small = count_queries { get game_access_codes_path(game) }

    6.times { AccessCode.generate_batch!(:game => game, :count => 2, :issued_by => operator) }
    large = count_queries { get game_access_codes_path(game) }

    expect(large).to eq(small)
  end
end
```

`count_queries` already exists as a helper in this repo's request specs — `grep -rn "def count_queries" spec/` finds it. Use it rather than writing another; if it is defined privately inside one spec file, follow how `spec/requests/admin_console_spec.rb` gets at it.

- [ ] **Step 2: Run it and confirm it fails for the right reason**

Run: `bundle exec rspec spec/requests/access_code_console_spec.rb`
Expected: FAIL — `undefined local variable or method 'lookup_game_access_codes_path'`, and the listing examples failing on missing markup.

- [ ] **Step 3: Add the route**

In `config/routes.rb`, in the `access_codes` collection block beside the three PATCHes:

```ruby
        # POST, not GET: a GET would put the customer's secret in a query
        # string, and that lands in server logs, browser history and every
        # proxy between -- the one place filter_parameters cannot reach.
        post :lookup
```

- [ ] **Step 4: Make `#index` group, and add `#lookup`**

Replace `#index` in `app/controllers/access_codes_controller.rb`:

```ruby
  # Batches, not codes -- there is nothing readable to show per code, and the
  # counts are what an operator actually asks for.
  #
  # THREE grouped queries for the whole page regardless of how many batches
  # exist, not one lookup per batch: sub-project B broke two query-count specs
  # by adding a per-row read behind a listing, and this screen has its own.
  def index
    scope = @game.access_codes
    @batches   = scope.group(:batch_key).minimum(:created_at)
    @sizes     = scope.group(:batch_key).count
    @redeemed  = scope.where.not(:redeemed_at => nil).group(:batch_key).count
    @revoked   = scope.where(:redeemed_at => nil).where.not(:revoked_at => nil).group(:batch_key).count
    @expired   = scope.where(:redeemed_at => nil, :revoked_at => nil)
                      .where("expires_at IS NOT NULL AND expires_at <= ?", Time.now)
                      .group(:batch_key).count
  end
```

and add, beside the state actions:

```ruby
  # An operator cannot read codes off this screen, so the only way to answer
  # "my code does not work" is for the customer to supply the secret and for
  # us to digest it exactly as redemption does. That is the whole support
  # workflow, and it is why AccessCode.normalize is one method rather than
  # two: if this and redemption ever disagreed about confusables, an operator
  # would confirm a code is fine while the customer kept failing to use it.
  def lookup
    code = AccessCode.find_by_code(params[:access_code])
    @found = code if code && code.game_id == @game.id
    index
    render :index
  end
```

- [ ] **Step 5: Extend the view**

Add to `app/views/access_codes/index.html.erb`, after the generation form:

```erb
<h3><%= t("access_codes.lookup.title") %></h3>

<%# POST, not GET: a GET would put the customer's secret in a query string,
    which reaches server logs, browser history and every proxy between --
    the one place filter_parameters cannot help. %>
<%= form_with url: lookup_game_access_codes_path(@game), method: :post, local: true do %>
  <p>
    <%= text_field_tag :access_code, nil, :autocomplete => "off", :autocapitalize => "characters" %>
    <%= submit_tag t("access_codes.lookup.submit"), :class => "btn" %>
  </p>
<% end %>

<% if params[:access_code].present? %>
  <% if @found %>
    <%# Facts ABOUT the code, never the code: an operator's screenshot or
        screen-share must not be able to leak one. %>
    <p>
      <%= t("access_codes.batch") %>: <%= @found.batch_key %> &middot;
      <%= t("access_codes.#{@found.state}") %>
      <% if @found.access_pass&.team %>
        &middot; <%= t("access_codes.lookup.team") %>: <%= @found.access_pass.team.name %>
      <% end %>
    </p>
    <% unless @found.redeemed_at %>
      <%= button_to t("access_codes.revoke"), revoke_game_access_codes_path(@game),
                    :method => :patch, :params => { :code_id => @found.id }, :class => "btn btn--danger" %>
    <% end %>
  <% else %>
    <p class="alert"><%= t("access_codes.lookup.not_found") %></p>
  <% end %>
<% end %>

<h3><%= t("access_codes.title") %></h3>

<div class="table-wrap">
<table class="table--cards">
  <thead>
    <tr>
      <th><%= t("access_codes.batch") %></th>
      <th><%= t("access_codes.created_at") %></th>
      <th><%= t("access_codes.size") %></th>
      <th><%= t("access_codes.outstanding") %></th>
      <th><%= t("access_codes.redeemed") %></th>
      <th><%= t("access_codes.revoked") %></th>
      <th><%= t("access_codes.expired") %></th>
      <th></th>
    </tr>
  </thead>
  <tbody>
  <% @batches.each do |key, created| %>
    <% size = @sizes[key].to_i %>
    <% used = @redeemed[key].to_i %>
    <% gone = @revoked[key].to_i %>
    <% old  = @expired[key].to_i %>
    <tr>
      <td data-label="<%= t("access_codes.batch") %>"><%= key %></td>
      <td data-label="<%= t("access_codes.created_at") %>"><%= l(created, :format => :long) %></td>
      <td data-label="<%= t("access_codes.size") %>"><%= size %></td>
      <td data-label="<%= t("access_codes.outstanding") %>"><%= size - used - gone - old %></td>
      <td data-label="<%= t("access_codes.redeemed") %>"><%= used %></td>
      <td data-label="<%= t("access_codes.revoked") %>"><%= gone %></td>
      <td data-label="<%= t("access_codes.expired") %>"><%= old %></td>
      <td>
        <%# No confirmation attributes: this app ships neither Turbo nor
            rails-ujs, so data-confirm would imply a dialog nobody gets. %>
        <%= button_to t("access_codes.revoke"), revoke_game_access_codes_path(@game),
                      :method => :patch, :params => { :batch_key => key }, :class => "btn btn--danger" %>
        <%= button_to t("access_codes.unrevoke"), unrevoke_game_access_codes_path(@game),
                      :method => :patch, :params => { :batch_key => key }, :class => "btn" %>
        <%= form_with url: expiry_game_access_codes_path(@game), method: :patch, local: true do %>
          <%= hidden_field_tag :batch_key, key %>
          <%= date_field_tag :expires_at %>
          <%= submit_tag t("access_codes.set_expiry"), :class => "btn" %>
        <% end %>
      </td>
    </tr>
  <% end %>
  </tbody>
</table>
</div>
```

`data-label` and `table--cards` are how this app's tables collapse on a phone — read `app/views/admin/users/index.html.erb` and match it.

**Never render a raw code in this file.** The console holds none; the lookup shows facts *about* a code so that an operator's screenshot cannot leak one.

Two more keys are needed for the buttons, in all seven locales:

| Key | ru | en | uk | ka | tr | be | pl |
|---|---|---|---|---|---|---|---|
| `access_codes.revoke` | Отозвать | Revoke | Відкликати | ჩამორთმევა | Geri al | Адклікаць | Odbierz |
| `access_codes.unrevoke` | Вернуть | Restore | Повернути | დაბრუნება | Geri ver | Вярнуць | Przywróć |
| `access_codes.set_expiry` | Срок | Expiry | Термін | ვადა | Süre | Тэрмін | Termin |

And the four state words the lookup renders via `t("access_codes.#{@found.state}")` are the same
keys the table headers use — `outstanding`, `redeemed`, `revoked`, `expired` — so they need no
additions beyond Step 6's table.

- [ ] **Step 6: Add the keys to all seven locales**

| Key | ru | en |
|---|---|---|
| `access_codes.batch` | Партия | Batch |
| `access_codes.created_at` | Создана | Created |
| `access_codes.size` | Всего | Total |
| `access_codes.outstanding` | Не использованы | Outstanding |
| `access_codes.redeemed` | Использованы | Redeemed |
| `access_codes.revoked` | Отозваны | Revoked |
| `access_codes.expired` | Просрочены | Expired |
| `access_codes.lookup.title` | Найти код | Find a code |
| `access_codes.lookup.submit` | Найти | Find |
| `access_codes.lookup.not_found` | Такой код не найден в этой игре | No such code in this game |
| `access_codes.lookup.team` | Использован командой | Redeemed by |

The remaining five, same order:

- `uk`: Партія / Створена / Усього / Не використані / Використані / Відкликані / Прострочені / Знайти код / Знайти / Такий код не знайдено в цій грі / Використаний командою
- `ka`: პარტია / შექმნილია / სულ / გამოუყენებელი / გამოყენებული / ჩამორთმეული / ვადაგასული / კოდის მოძებნა / მოძებნა / ასეთი კოდი ამ თამაშში ვერ მოიძებნა / გამოიყენა გუნდმა
- `tr`: Parti / Oluşturuldu / Toplam / Kullanılmadı / Kullanıldı / Geri alındı / Süresi doldu / Kod bul / Bul / Bu oyunda böyle bir kod yok / Kullanan takım
- `be`: Партыя / Створана / Усяго / Не выкарыстаны / Выкарыстаны / Адкліканы / Пратэрмінаваны / Знайсці код / Знайсці / Такі код не знойдзены ў гэтай гульні / Выкарыстаны камандай
- `pl`: Partia / Utworzona / Łącznie / Niewykorzystane / Wykorzystane / Odebrane / Przeterminowane / Znajdź kod / Znajdź / Nie znaleziono takiego kodu w tej grze / Wykorzystany przez drużynę

- [ ] **Step 7: Run the specs**

Run: `bundle exec rspec spec/requests/access_code_console_spec.rb spec/requests/access_code_generation_spec.rb spec/requests/access_code_state_spec.rb spec/i18n_spec.rb`
Expected: 0 failures.

- [ ] **Step 8: Commit**

```bash
git add app/controllers/access_codes_controller.rb app/views/access_codes \
        config/routes.rb spec/requests/access_code_console_spec.rb \
        config/locales/ru.yml config/locales/en.yml config/locales/uk.yml \
        config/locales/ka.yml config/locales/tr.yml config/locales/be.yml \
        config/locales/pl.yml
git commit -m "Add the batch list and the code lookup

The list shows batches and counts, from grouped queries whose number
does not grow with the number of batches -- this branch's sibling broke
two query-count specs by adding a per-row read behind a listing.

The lookup is the support workflow that digest-only storage takes away
and has to hand back: the customer supplies the secret, we digest it
exactly as redemption does, and show facts about the code without ever
rendering the code. It is a POST, because a GET would put that secret
in a query string and therefore in logs, history and every proxy
between.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>"
```

---

## Final Verification

Run by the controller, not by a task subagent. `bin/rails db:test:prepare` before **each** suite — the two share `db/test.sqlite3`, and a contract run straight after RSpec has produced phantom failures in this programme before.

- [ ] **Full RSpec suite** — `bundle exec rspec`. Expected: 0 failures. Measure the total; do not quote one from `CLAUDE.md`.

- [ ] **The inherited Cucumber contract:**

```bash
git ls-tree -r --name-only d035146 | grep '\.feature$' | sort > /tmp/inherited
git ls-files 'features/**/*.feature' | sort > /tmp/current
bundle exec cucumber $(comm -12 /tmp/inherited /tmp/current | tr '\n' ' ')
```

Expected: **228 scenarios (226 passed, 2 undefined) / 2325 steps**, unchanged. This plan edits no `.feature` file.

- [ ] **Whole Cucumber suite** — `bundle exec cucumber`. Expected: 0 failures.

- [ ] **Autoloading** — `bin/rails zeitwerk:check`. Expected: "All is good!"

- [ ] **Production boot:**

```bash
RAILS_ENV=production SECRET_KEY_BASE=x APP_HOST=example.com SMTP_USERNAME=u \
SMTP_PASSWORD=p SMTP_ADDRESS=s MAIL_FROM=m@e.com \
DATABASE_URL="sqlite3:/tmp/probe.sqlite3" bin/rails runner 'puts "ok"'
```

- [ ] **No raw code can reach a log.** Confirm `:access_code` is in `config.filter_parameters`, that no `Rails.logger` call in the new controllers takes the parameter, and that the lookup route is POST:

```bash
grep -n "filter_parameters" config/application.rb
bin/rails routes | grep lookup
grep -rn "params\[:access_code\]" app/ | grep -i "logger\|raise"
```

The third command must print nothing.

- [ ] **No raw code is rendered by the console** — `grep -n "raw\|code_digest" app/views/access_codes/index.html.erb` must show no interpolation of a code value; only `created.html.erb` renders codes, and only from the in-memory array.

- [ ] **Locale completeness** — `spec/i18n_spec.rb` proves only `ru`↔`en`, so check the other five by hand:

```bash
for k in access_codes access_code_redemptions; do
  echo "== $k"; grep -L "$k:" config/locales/{ru,en,uk,ka,tr,be,pl}.yml
done
```

Any file printed is missing that block.
