# Operator Adjustments Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Let an operator add or deduct points for a team, on one run or globally, publicly and with a reason.

**Architecture:** A fourth ledger reason, `adjustment`, written by a new `PointTransaction.adjust!`
that deliberately does **not** rescue duplicates. `game_passing_id` and `game_id` become nullable so
a row can belong to the team rather than to a run, and the per-attempt unique index is narrowed so
a team can be adjusted more than once.

**Tech Stack:** Rails 8.0, Ruby 3.3.12, SQLite in dev/test and PostgreSQL in production, RSpec,
Cucumber (Russian Gherkin), seven locales.

**Spec:** `docs/superpowers/specs/2026-08-19-operator-adjustments-design.md`

## Global Constraints

- **Ruby is not on `PATH` in non-login shells.** Every session starts with
  `export PATH="$HOME/.rbenv/bin:$HOME/.rbenv/shims:$PATH"`.
- **Hash rockets** (`:key => value`) in application code; match the surrounding file.
- **Never edit any `features/**/*.feature` file** — a byte-identical acceptance contract of
  **228 scenarios / 2325 steps**. If one fails, the change is wrong.
- **All seven locales** for every new key: `ru`, `en`, `uk`, `ka`, `tr`, `be`, `pl`.
  `spec/i18n_spec.rb` enforces exact `ru`↔`en` parity and only a **subset** check for the other
  five, so a key missing from `uk`/`ka`/`tr`/`be`/`pl` leaves the suite green.
- **`spec/i18n_audit_actions_spec.rb` reads audit action names out of the controllers**, so a
  missing `admin.audit.index.action.*` label fails that spec rather than rendering the raw
  identifier.
- **A validation message is a predicate, not a sentence.** `ApplicationHelper#error_messages_for`
  composes `"%{attribute} %{message}"`, so every new validation needs an attribute **noun** under
  `activerecord.attributes` and a **predicate** under `activerecord.errors.models`, agreeing in
  gender in `ru`, `uk`, `be` and `pl`. **Render the composed sentence before trusting it** — nothing
  in this repository tests that two separately-correct fragments make one correct sentence.
- **The operator's note is operator-authored content: render it verbatim, never through `t()`.**
- **No Turbo and no rails-ujs.** `data: { confirm: ... }` is inert markup.
- **`db/schema.rb` is regenerated by running the migration**, never hand-edited.
- **Migration timestamps** must not be more than a day in the future; `master` already holds
  `20260819110000`.
- **`create_user` takes no arguments**; **`create_team(options)` takes `:captain` and `:members`**;
  **`create_game_passing` calls `create_team` with no options, so its team has no captain.**
- **A request spec without `set_game_schedule!` gets 401s** — `starts_at` lives on `game_runs`, not
  `games`, and defaults to 2099. **Every negative assertion needs a positive status assertion beside
  it.** Ten examples in the preceding sub-projects passed against vacuous fixtures, and this was the
  commonest cause.
- **Assert row counts, not balances** — a wrong sign and a missing row cancel.
- TDD: the failing test first, watch it fail for the stated reason, then implement.

---

## File structure

| File | Responsibility |
|---|---|
| `db/migrate/20260819120000_allow_global_point_transactions.rb` | nullability, `note`, the narrowed index |
| `app/models/point_transaction.rb` | the `adjustment` reason, optional associations, validations, `adjust!` |
| `app/controllers/interventions_controller.rb` | the game-scoped adjustment |
| `app/controllers/admin/team_adjustments_controller.rb` | the global adjustment, superadmin-only |
| `app/views/interventions/new_adjustment.html.erb`, `confirm_adjustment.html.erb` | the game-scoped form and its confirmation |
| `app/views/admin/team_adjustments/new.html.erb`, `confirm.html.erb` | the global form and its confirmation |
| `app/controllers/teams_controller.rb`, `app/views/teams/show.html.erb` | the game-less section |
| `config/locales/*.yml` | seven files, every new key |

---

## Task 1: The `adjustment` row

**Files:**
- Create: `db/migrate/20260819120000_allow_global_point_transactions.rb`
- Modify: `app/models/point_transaction.rb`
- Modify: `config/locales/{ru,en,uk,ka,tr,be,pl}.yml`
- Test: `spec/models/point_transaction/adjustment_spec.rb`

**Interfaces:**
- Produces: `PointTransaction.adjust!(team:, amount:, note:, actor:, passing: nil)` → the created
  row. Raises on failure; **does not rescue**. `PointTransaction#adjustment?` → Boolean.
  `REASONS` gains `"adjustment"`. `point_transactions.note` (text, nullable at the database).

- [ ] **Step 1: Write the failing test**

Create `spec/models/point_transaction/adjustment_spec.rb`:

```ruby
require "rails_helper"

describe PointTransaction do
  let(:actor) { create_user }

  describe ".adjust!" do
    it "writes a global row with no game and no attempt" do
      team = create_team(:captain => create_user)

      row = PointTransaction.adjust!(:team => team, :amount => -50,
                                     :note => "Ушли с точки, не дождавшись судьи",
                                     :actor => actor)

      expect(row.game_id).to be_nil
      expect(row.game_passing_id).to be_nil
      expect(row.level_id).to be_nil
      expect(row.team_id).to eq(team.id)
      expect(row.reason).to eq("adjustment")
      expect(row.created_by_id).to eq(actor.id)
      expect(team.balance).to eq(-50)
    end

    it "denormalises the game and team from a passing when one is given" do
      passing = create_game_passing

      row = PointTransaction.adjust!(:team => passing.team, :amount => 20,
                                     :note => "Точка была закрыта", :actor => actor,
                                     :passing => passing)

      expect(row.game_passing_id).to eq(passing.id)
      expect(row.game_id).to eq(passing.game_id)
      expect(row.team_id).to eq(passing.team_id)
    end

    # The index change. Without it the second call raises RecordNotUnique,
    # because the per-attempt index covers every reason with a NULL level_id.
    it "allows more than one adjustment on the same attempt" do
      passing = create_game_passing

      PointTransaction.adjust!(:team => passing.team, :amount => -10, :note => "a",
                               :actor => actor, :passing => passing)
      PointTransaction.adjust!(:team => passing.team, :amount => -10, :note => "b",
                               :actor => actor, :passing => passing)

      expect(PointTransaction.where(:game_passing_id => passing.id,
                                    :reason => "adjustment").count).to eq(2)
    end

    it "allows more than one global adjustment for the same team" do
      team = create_team(:captain => create_user)

      PointTransaction.adjust!(:team => team, :amount => 5,  :note => "a", :actor => actor)
      PointTransaction.adjust!(:team => team, :amount => -5, :note => "b", :actor => actor)

      expect(team.point_transactions.count).to eq(2)
      expect(team.balance).to eq(0)
    end

    # The narrowed index is SHARED with three reasons that depend on it for
    # idempotence. Narrowing it for adjustments must not have narrowed it for
    # them -- and an example per reason, because `reason <> 'adjustment'` is one
    # clause protecting three separate guarantees, and a typo in it would
    # release all three at once while every adjustment example stayed green.
    %w[level_completed game_completed].each do |reason|
      it "still refuses a duplicate #{reason} on the same attempt" do
        passing = create_game_passing
        level   = reason == "level_completed" ? passing.current_level : nil

        first  = PointTransaction.award!(:passing => passing, :reason => reason,
                                         :level => level, :amount => 10)
        second = PointTransaction.award!(:passing => passing, :reason => reason,
                                         :level => level, :amount => 10)

        expect(first).to be_persisted
        expect(second).to be_nil
        expect(PointTransaction.where(:game_passing_id => passing.id,
                                      :reason => reason).count).to eq(1)
      end
    end

    it "still refuses a duplicate level_skipped on the same level" do
      passing = create_game_passing
      level   = passing.current_level

      first  = PointTransaction.award!(:passing => passing, :reason => "level_skipped",
                                       :level => level, :amount => -5)
      second = PointTransaction.award!(:passing => passing, :reason => "level_skipped",
                                       :level => level, :amount => -5)

      expect(first).to be_persisted
      expect(second).to be_nil
    end

    # A5: award! rescues RecordNotUnique and returns nil, which is right for an
    # idempotent award and wrong for an adjustment. adjust! must raise.
    it "does not swallow a constraint violation" do
      team = create_team(:captain => create_user)
      allow(PointTransaction).to receive(:create!)
        .and_raise(ActiveRecord::RecordNotUnique.new("boom"))

      expect {
        PointTransaction.adjust!(:team => team, :amount => 1, :note => "x", :actor => actor)
      }.to raise_error(ActiveRecord::RecordNotUnique)
    end
  end

  describe "validations on adjustment rows" do
    it "requires a note" do
      team = create_team(:captain => create_user)
      row  = PointTransaction.new(:team => team, :amount => -1, :reason => "adjustment")

      expect(row).not_to be_valid
      expect(row.errors[:note]).not_to be_empty
    end

    it "refuses a zero amount" do
      team = create_team(:captain => create_user)
      row  = PointTransaction.new(:team => team, :amount => 0, :reason => "adjustment",
                                  :note => "x")

      expect(row).not_to be_valid
      expect(row.errors[:amount]).not_to be_empty
    end

    # The validations are conditional on the reason, so every row written by
    # D1 and D2 -- none of which has a note -- must still be valid.
    it "does not impose either rule on an award" do
      passing = create_game_passing
      row = PointTransaction.award!(:passing => passing, :reason => "level_completed",
                                    :level => passing.current_level, :amount => 0)

      expect(row).to be_persisted
      expect(row.note).to be_nil
    end

    # Relaxing the two belongs_to associations to :optional must not relax them
    # for the writers that should still require them.
    it "still refuses an award with no passing" do
      row = PointTransaction.new(:team => create_team(:captain => create_user),
                                 :amount => 10, :reason => "level_completed")

      expect(row).not_to be_valid
      expect(row.errors[:game_passing]).not_to be_empty
    end
  end
end
```

- [ ] **Step 2: Run it and watch it fail**

Run: `bundle exec rspec spec/models/point_transaction/adjustment_spec.rb`
Expected: FAIL — `undefined method 'adjust!' for PointTransaction`.

- [ ] **Step 3: Write the migration**

Create `db/migrate/20260819120000_allow_global_point_transactions.rb`:

```ruby
class AllowGlobalPointTransactions < ActiveRecord::Migration[8.0]
  def change
    # A team-global adjustment belongs to no run and no game.
    change_column_null :point_transactions, :game_passing_id, true
    change_column_null :point_transactions, :game_id, true

    # text, not string: an operator explaining a disputed penalty writes a few
    # sentences, and a 255-character ceiling would truncate exactly the
    # adjustments that most need explaining.
    add_column :point_transactions, :note, :text

    # The per-attempt index caps one row per (attempt, reason). That is what
    # makes an award idempotent, and it would cap adjustments at ONE PER
    # ATTEMPT forever. Narrow it rather than dropping it: level_completed,
    # game_completed and level_skipped all still depend on it.
    #
    # Global adjustments were never affected -- NULLs are distinct in a unique
    # index -- so without this change the feature would work for the rarer case
    # and silently refuse the commoner one, and work the first time in both.
    remove_index :point_transactions,
                 :column => [ :game_passing_id, :reason ],
                 :name   => "index_point_transactions_per_attempt",
                 :unique => true,
                 :where  => "level_id IS NULL"

    add_index :point_transactions, [ :game_passing_id, :reason ],
              :unique => true,
              :where  => "level_id IS NULL AND reason <> 'adjustment'",
              :name   => "index_point_transactions_per_attempt"
  end
end
```

- [ ] **Step 4: Run the migration**

Run: `bin/rails db:migrate && bin/rails db:test:prepare`
Expected: `db/schema.rb` shows `note`, both foreign keys without `null: false`, and the index's
`where` clause carrying `AND reason <> 'adjustment'`. Do not hand-edit the dump.

- [ ] **Step 5: Change the model**

In `app/models/point_transaction.rb`:

```ruby
  REASONS = %w[level_completed game_completed level_skipped adjustment].freeze
```

Change the two associations — **`belongs_to` is required by default since Rails 5, so a nullable
column alone still refuses the row**, which reads like a bug in `adjust!` rather than a missing
option because the migration plainly succeeded:

```ruby
  belongs_to :game,         :optional => true
  belongs_to :game_passing, :optional => true
```

Add the conditional validations and the predicate:

```ruby
  # Both conditional on the reason: every row D1 and D2 write has no note, and
  # a zero-amount award is legitimate (a game scoring 0 per level still records
  # that the level was passed).
  validates :note,   :presence => true,                            :if => :adjustment?
  validates :amount, :numericality => { :other_than => 0 },        :if => :adjustment?

  def adjustment?
    self.reason == "adjustment"
  end
```

And the writer, beside `award!`:

```ruby
  # NOT award!, and the difference is the point. award! rescues
  # RecordNotUnique and returns nil, which is what makes an award idempotent --
  # a level re-passed after an operator sent a team back awards once. Two
  # deliberate -50s are two different events, not a retry of one.
  #
  # After the index change above a violation cannot fire here at all, and that
  # is exactly what would make reusing award! dangerous rather than harmless:
  # this method would carry a rescue that is currently unreachable and would
  # silently swallow a real constraint error the day anything about that index
  # changes.
  #
  # passing nil => a global row: game_id and game_passing_id both NULL, and the
  # row belongs to the team rather than to any run.
  def self.adjust!(team:, amount:, note:, actor:, passing: nil)
    create!(:team_id         => team.id,
            :game_id         => passing&.game_id,
            :game_passing_id => passing&.id,
            :level_id        => nil,
            :amount          => amount,
            :reason          => "adjustment",
            :note            => note,
            :created_by      => actor)
  end
```

- [ ] **Step 6: Run the test to verify it passes**

Run: `bundle exec rspec spec/models/point_transaction spec/models/game_passing`
Expected: PASS. The `game_passing` specs are in the command because Task 1 relaxes two associations
they depend on.

- [ ] **Step 7: Add the locale keys**

Under `activerecord:` → `attributes:`, a new `point_transaction:` block; and under
`activerecord:` → `errors:` → `models:`, a new `point_transaction:` block. Both nouns are chosen so
the composed sentence agrees — in `ru` `Сумма` is feminine and takes `не может быть нулевой`, while
`Пояснение` is neuter and takes `не указано`.

| locale | attr `amount` | attr `note` | `amount.other_than` | `note.blank` |
|---|---|---|---|---|
| ru | `Сумма` | `Пояснение` | `не может быть нулевой` | `не указано` |
| en | `Amount` | `Explanation` | `cannot be zero` | `is required` |
| uk | `Сума` | `Пояснення` | `не може бути нульовою` | `не вказано` |
| be | `Сума` | `Тлумачэнне` | `не можа быць нулявой` | `не пазначана` |
| pl | `Kwota` | `Wyjaśnienie` | `nie może być zerowa` | `nie zostało podane` |
| tr | `Miktar` | `Açıklama` | `sıfır olamaz` | `girilmelidir` |
| ka | `ოდენობა` | `განმარტება` | `ნულოვანი ვერ იქნება` | `არ არის მითითებული` |

And the ledger reason label, beside the existing `level_completed` / `game_completed` /
`level_skipped` under the same `reasons:` block:

| locale | `adjustment` |
|---|---|
| ru | `Корректировка` |
| en | `Adjustment` |
| uk | `Коригування` |
| be | `Карэкціроўка` |
| pl | `Korekta` |
| tr | `Düzeltme` |
| ka | `კორექტირება` |

- [ ] **Step 8: Render the composed error sentences**

Run, and read the output rather than only checking it does not raise:

```bash
bin/rails runner '
  I18n.locale = :ru
  t = Team.new(:name => "x")
  r = PointTransaction.new(:team => t, :amount => 0, :reason => "adjustment")
  r.valid?
  puts r.errors.full_messages
'
```

Expected: two grammatical Russian sentences. `Сумма не может быть нулевой` and
`Пояснение не указано`. If either reads wrongly, the noun and the predicate disagree in gender —
fix the pair, not just one half. Paste the output into your report.

- [ ] **Step 9: Run the tests**

Run: `bundle exec rspec spec/models/point_transaction spec/models/game_passing spec/i18n_spec.rb spec/requests/team_history_spec.rb spec/requests/points_chart_spec.rb`
Expected: PASS. The last two are in the command because they render every reason label and
aggregate every row.

- [ ] **Step 10: Commit**

```bash
git add db/migrate/20260819120000_allow_global_point_transactions.rb db/schema.rb \
        app/models/point_transaction.rb config/locales \
        spec/models/point_transaction/adjustment_spec.rb
git commit -m "Add the adjustment ledger row"
```

---

## Task 2: The game-scoped adjustment

**Files:**
- Modify: `config/routes.rb` (beside the team-scoped interventions)
- Modify: `app/controllers/interventions_controller.rb`
- Create: `app/views/interventions/new_adjustment.html.erb`, `app/views/interventions/confirm_adjustment.html.erb`
- Modify: `app/views/game_passings/_intervention_controls.html.erb`
- Modify: `config/locales/{ru,en,uk,ka,tr,be,pl}.yml`
- Test: `spec/requests/game_adjustment_spec.rb`

**Interfaces:**
- Consumes: `PointTransaction.adjust!`.

**The single most likely thing to get wrong in this sub-project** is the `ensure_game_is_live`
filter. It is declared for the whole controller, and spec A7 requires adjustments to work on
**finished and exited** runs — a dispute settled the next morning is the commonest adjustment there
is. Inheriting the filter looks correct and fails exactly the case A7 exists to serve.

- [ ] **Step 1: Write the failing test**

Create `spec/requests/game_adjustment_spec.rb`:

```ruby
require "rails_helper"

describe "an operator adjusting a team's points for one game", type: :request do
  def sign_in(user)
    put login_path, :params => { :email => user.email, :password => "1234" }
  end

  def live_game_with_passing(attrs = {})
    author  = attrs[:author] || create_user
    game    = create_game(:author => author)
    level   = create_level(:game => game, :position => 1)
    create_level(:game => game, :position => 2)
    set_game_schedule!(game, :starts_at => 1.hour.ago)
    passing = create_game_passing(:level => level)
    [ author, game, passing ]
  end

  it "shows a form, then a confirmation, then writes on the confirmed post" do
    author, game, passing = live_game_with_passing
    sign_in(author)

    get new_team_adjustment_path(:game_id => game.id, :team_id => passing.team_id)
    expect(response).to have_http_status(:ok)

    post team_adjustments_path(:game_id => game.id, :team_id => passing.team_id),
         :params => { :amount => -50, :note => "Пропустили точку" }
    expect(response).to have_http_status(:ok)
    expect(response.body).to include("-50")
    expect(response.body).to include("Пропустили точку")
    expect(PointTransaction.where(:reason => "adjustment").count).to eq(0)

    post team_adjustments_path(:game_id => game.id, :team_id => passing.team_id),
         :params => { :amount => -50, :note => "Пропустили точку", :confirmed => "1" }

    row = PointTransaction.find_by(:reason => "adjustment")
    expect(row.amount).to eq(-50)
    expect(row.note).to eq("Пропустили точку")
    expect(row.game_passing_id).to eq(passing.id)
    expect(row.created_by_id).to eq(author.id)
  end

  # A7. This is the example the whole task turns on.
  it "adjusts a FINISHED run" do
    author, game, passing = live_game_with_passing
    passing.update!(:finished_at => 1.hour.ago)
    sign_in(author)

    post team_adjustments_path(:game_id => game.id, :team_id => passing.team_id),
         :params => { :amount => 30, :note => "Точка была закрыта", :confirmed => "1" }

    expect(PointTransaction.where(:reason => "adjustment").count).to eq(1)
  end

  it "adjusts an EXITED run" do
    author, game, passing = live_game_with_passing
    passing.exit!
    sign_in(author)

    post team_adjustments_path(:game_id => game.id, :team_id => passing.team_id),
         :params => { :amount => 30, :note => "Компенсация", :confirmed => "1" }

    expect(PointTransaction.where(:reason => "adjustment").count).to eq(1)
  end

  it "refuses a blank note without writing anything" do
    author, game, passing = live_game_with_passing
    sign_in(author)

    post team_adjustments_path(:game_id => game.id, :team_id => passing.team_id),
         :params => { :amount => -50, :note => "", :confirmed => "1" }

    expect(PointTransaction.where(:reason => "adjustment").count).to eq(0)
  end

  it "refuses an ordinary user" do
    _author, game, passing = live_game_with_passing
    sign_in(create_user)

    post team_adjustments_path(:game_id => game.id, :team_id => passing.team_id),
         :params => { :amount => -50, :note => "x", :confirmed => "1" }

    expect(PointTransaction.where(:reason => "adjustment").count).to eq(0)
  end
end
```

- [ ] **Step 2: Run it and watch it fail**

Run: `bundle exec rspec spec/requests/game_adjustment_spec.rb`
Expected: FAIL — `undefined local variable or method 'new_team_adjustment_path'`.

- [ ] **Step 3: Add the routes**

In `config/routes.rb`, beside the other team-scoped interventions:

```ruby
  # Two endpoints, not three: the POST renders a confirmation when `confirmed`
  # is absent and writes when it is present. This app has no rails-ujs, so a
  # data-confirm dialog would be inert markup and the click would go straight
  # through -- and an adjustment cannot be undone (the ledger never reverses).
  get  "/games/:game_id/teams/:team_id/adjustments/new", to: "interventions#new_adjustment", as: :new_team_adjustment
  post "/games/:game_id/teams/:team_id/adjustments",     to: "interventions#create_adjustment", as: :team_adjustments
```

- [ ] **Step 4: Add the actions and the filter exemption**

In `app/controllers/interventions_controller.rb`, extend `find_game_passing` and — the critical
line — exempt both new actions from `ensure_game_is_live`:

```ruby
  before_action :find_game_passing,
                only: [ :move, :reinstate, :reset_clock, :skip_level,
                        :new_adjustment, :create_adjustment ]

  # A7: an adjustment is usually a judgement made AFTER a run -- a dispute
  # settled the next morning, a location confirmed broken once the game is
  # over. The commonest adjustment is one nobody could have made while the run
  # was live, so these two actions are the only ones here that must survive a
  # game that has ended.
  skip_before_action :ensure_game_is_live, only: [ :new_adjustment, :create_adjustment ]
```

and the actions:

```ruby
  def new_adjustment
    @amount = nil
    @note   = nil
  end

  def create_adjustment
    @amount = params[:amount].to_i
    @note   = params[:note].to_s

    return render(:confirm_adjustment) if params[:confirmed].blank?

    PointTransaction.adjust!(:team => @game_passing.team, :amount => @amount,
                             :note => @note, :actor => current_user,
                             :passing => @game_passing)
    audit("adjust_points", @game_passing.team.name)
    back_to_stats(t("interventions.adjusted_notice"))
  rescue ActiveRecord::RecordInvalid
    render :new_adjustment, :status => :unprocessable_entity
  end
```

`rescue ActiveRecord::RecordInvalid` and not the controller's `rescue_from ArgumentError`: a blank
note is a form error the operator can correct on the spot, not a refusal to report on the stats
page.

- [ ] **Step 5: Write the two views**

`app/views/interventions/new_adjustment.html.erb`:

```erb
<div class="page">
  <h2><%= t("interventions.adjust.title", :team => @game_passing.team.name) %></h2>

  <%= form_with url: team_adjustments_path(:game_id => @game.id, :team_id => @game_passing.team_id), method: :post do |f| %>
    <div class="field">
      <%= label_tag :amount, t("interventions.adjust.amount") %>
      <%= number_field_tag :amount, @amount %>
    </div>
    <div class="field">
      <%= label_tag :note, t("interventions.adjust.note") %>
      <%= text_area_tag :note, @note %>
    </div>
    <%= submit_tag t("interventions.adjust.review"), :class => "btn" %>
  <% end %>
</div>
```

`app/views/interventions/confirm_adjustment.html.erb`:

```erb
<div class="page">
  <h2><%= t("interventions.adjust.confirm_title") %></h2>

  <%# The team is named here deliberately: the mistake a confirmation catches
      is not a mistyped amount, which the operator can see, but an amount
      entered against the wrong team. %>
  <p><%= t("interventions.adjust.summary",
           :team => @game_passing.team.name, :amount => @amount) %></p>

  <%# Operator-authored text: rendered verbatim, never through t(). %>
  <p class="adjust-note"><%= @note %></p>

  <p><%= t("interventions.adjust.balance_change",
           :before => @game_passing.team.balance,
           :after  => @game_passing.team.balance + @amount) %></p>

  <%= form_with url: team_adjustments_path(:game_id => @game.id, :team_id => @game_passing.team_id), method: :post do %>
    <%= hidden_field_tag :amount, @amount %>
    <%= hidden_field_tag :note, @note %>
    <%= hidden_field_tag :confirmed, "1" %>
    <%= submit_tag t("interventions.adjust.confirm"), :class => "btn danger-row" %>
  <% end %>

  <%= link_to t("interventions.adjust.cancel"),
              new_team_adjustment_path(:game_id => @game.id, :team_id => @game_passing.team_id) %>
</div>
```

- [ ] **Step 6: Add the control**

In `app/views/game_passings/_intervention_controls.html.erb`, following the wrapper the existing
`move`/`reinstate` controls use — **read the file first**, it may use locals rather than instance
variables:

```erb
<%= link_to t("interventions.adjust.button"),
            new_team_adjustment_path(:game_id => game.id, :team_id => game_passing.team_id),
            :class => "btn" %>
```

Unconditional, deliberately: unlike a skip there is no cap and no per-game setting, so there is no
state in which an adjustment is unavailable.

- [ ] **Step 7: Add the locale keys**

Under `interventions:`, a new `adjust:` block, plus `adjusted_notice`:

| key | ru | en |
|---|---|---|
| `button` | `Корректировка очков` | `Adjust points` |
| `title` | `Корректировка очков команды «%{team}»` | `Adjust points for %{team}` |
| `amount` | `Сколько очков (минус — списание)` | `How many points (negative deducts)` |
| `note` | `Пояснение (будет видно всем)` | `Explanation (publicly visible)` |
| `review` | `Проверить` | `Review` |
| `confirm_title` | `Подтвердите корректировку` | `Confirm the adjustment` |
| `summary` | `Команде «%{team}»: %{amount} очков` | `%{amount} points for %{team}` |
| `balance_change` | `Баланс: %{before} → %{after}` | `Balance: %{before} → %{after}` |
| `confirm` | `Да, записать` | `Yes, record it` |
| `cancel` | `Изменить` | `Change it` |
| `adjusted_notice` | `Очки скорректированы` | `The points were adjusted` |

| key | uk | be | pl |
|---|---|---|---|
| `button` | `Коригування очок` | `Карэкціроўка ачкоў` | `Korekta punktów` |
| `title` | `Коригування очок команди «%{team}»` | `Карэкціроўка ачкоў каманды «%{team}»` | `Korekta punktów drużyny „%{team}”` |
| `amount` | `Скільки очок (мінус — списання)` | `Колькі ачкоў (мінус — спісанне)` | `Ile punktów (minus odejmuje)` |
| `note` | `Пояснення (буде видно всім)` | `Тлумачэнне (будзе бачна ўсім)` | `Wyjaśnienie (widoczne publicznie)` |
| `review` | `Перевірити` | `Праверыць` | `Sprawdź` |
| `confirm_title` | `Підтвердьте коригування` | `Пацвердзіце карэкціроўку` | `Potwierdź korektę` |
| `summary` | `Команді «%{team}»: %{amount} очок` | `Камандзе «%{team}»: %{amount} ачкоў` | `%{amount} punktów dla drużyny „%{team}”` |
| `balance_change` | `Баланс: %{before} → %{after}` | `Баланс: %{before} → %{after}` | `Bilans: %{before} → %{after}` |
| `confirm` | `Так, записати` | `Так, запісаць` | `Tak, zapisz` |
| `cancel` | `Змінити` | `Змяніць` | `Zmień` |
| `adjusted_notice` | `Очки скориговано` | `Ачкі скарэктаваны` | `Punkty zostały skorygowane` |

Turkish and Georgian put the case suffix on a **common noun**, never on `%{team}`:

| key | tr | ka |
|---|---|---|
| `button` | `Puan düzeltmesi` | `ქულების კორექტირება` |
| `title` | `«%{team}» adlı takımın puan düzeltmesi` | `გუნდის «%{team}» ქულების კორექტირება` |
| `amount` | `Kaç puan (eksi düşer)` | `რამდენი ქულა (მინუსი აკლებს)` |
| `note` | `Açıklama (herkese görünür)` | `განმარტება (ხილულია ყველასთვის)` |
| `review` | `Gözden geçir` | `შემოწმება` |
| `confirm_title` | `Düzeltmeyi onaylayın` | `დაადასტურეთ კორექტირება` |
| `summary` | `«%{team}» adlı takıma %{amount} puan` | `გუნდს «%{team}» — %{amount} ქულა` |
| `balance_change` | `Bakiye: %{before} → %{after}` | `ბალანსი: %{before} → %{after}` |
| `confirm` | `Evet, kaydet` | `დიახ, ჩაიწეროს` |
| `cancel` | `Değiştir` | `შეცვლა` |
| `adjusted_notice` | `Puanlar düzeltildi` | `ქულები კორექტირებულია` |

And the audit label, under `admin:` → `audit:` → `index:` → `action:`, beside `skip_level`:

| locale | `adjust_points` |
|---|---|
| ru | `Скорректировал очки команды` |
| en | `Adjusted a team's points` |
| uk | `Скоригував очки команди` |
| be | `Скарэктаваў ачкі каманды` |
| pl | `Skorygował punkty drużyny` |
| tr | `Bir takımın puanını düzeltti` |
| ka | `დაკორექტირდა გუნდის ქულები` |

- [ ] **Step 8: Run the tests**

Run: `bundle exec rspec spec/requests/game_adjustment_spec.rb spec/requests/interventions_spec.rb spec/i18n_spec.rb spec/i18n_audit_actions_spec.rb`
Expected: PASS. `spec/requests/interventions_spec.rb` is the regression for the
controller this task edits.

- [ ] **Step 9: Prove the filter exemption is load-bearing**

Remove the `skip_before_action :ensure_game_is_live` line, run
`bundle exec rspec spec/requests/game_adjustment_spec.rb`, and confirm the **finished-run** and
**exited-run** examples go RED. Restore it and confirm GREEN. Paste both into your report — that
line is the one thing in this task that a later tidy-up is most likely to remove.

- [ ] **Step 10: Commit**

```bash
git add config/routes.rb app/controllers/interventions_controller.rb \
        app/views/interventions app/views/game_passings/_intervention_controls.html.erb \
        config/locales spec/requests/game_adjustment_spec.rb
git commit -m "Let an operator adjust a team's points for one game"
```

---

## Task 3: The global adjustment

**Files:**
- Create: `app/controllers/admin/team_adjustments_controller.rb`
- Create: `app/views/admin/team_adjustments/new.html.erb`, `confirm.html.erb`
- Modify: `config/routes.rb`
- Modify: `app/views/admin/teams/index.html.erb`
- Modify: `config/locales/{ru,en,uk,ka,tr,be,pl}.yml`
- Test: `spec/requests/global_adjustment_spec.rb`

**Interfaces:**
- Consumes: `PointTransaction.adjust!` with `passing: nil`.

- [ ] **Step 1: Write the failing test**

Create `spec/requests/global_adjustment_spec.rb`:

```ruby
require "rails_helper"

describe "a superadmin adjusting a team's points globally", type: :request do
  def sign_in(user)
    put login_path, :params => { :email => user.email, :password => "1234" }
  end

  def superadmin
    user = create_user
    user.update!(:is_superadmin => true)
    user
  end

  it "writes a row belonging to no game" do
    team = create_team(:captain => create_user)
    admin = superadmin
    sign_in(admin)

    post admin_team_adjustments_path(:team_id => team.id),
         :params => { :amount => -25, :note => "Нарушение регламента", :confirmed => "1" }

    row = PointTransaction.find_by(:reason => "adjustment")
    expect(row.team_id).to eq(team.id)
    expect(row.game_id).to be_nil
    expect(row.game_passing_id).to be_nil
    expect(row.created_by_id).to eq(admin.id)
    expect(team.balance).to eq(-25)
  end

  it "confirms before writing" do
    team = create_team(:captain => create_user)
    sign_in(superadmin)

    post admin_team_adjustments_path(:team_id => team.id),
         :params => { :amount => -25, :note => "Нарушение регламента" }

    expect(response).to have_http_status(:ok)
    expect(response.body).to include(team.name)
    expect(PointTransaction.count).to eq(0)
  end

  it "refuses an operator" do
    team = create_team(:captain => create_user)
    operator = create_user
    operator.update!(:is_operator => true)
    sign_in(operator)

    post admin_team_adjustments_path(:team_id => team.id),
         :params => { :amount => -25, :note => "x", :confirmed => "1" }

    expect(PointTransaction.count).to eq(0)
  end

  it "refuses a signed-out visitor" do
    team = create_team(:captain => create_user)

    post admin_team_adjustments_path(:team_id => team.id),
         :params => { :amount => -25, :note => "x", :confirmed => "1" }

    expect(PointTransaction.count).to eq(0)
  end
end
```

`users.is_superadmin` and `users.is_operator` are the real boolean columns, read by
`User#superadmin?` (`app/models/user.rb:92`) and `User#operator?` (`:96`). `config/routes.rb:12`
already opens a `namespace :admin` block — add the nested resource there rather than opening a
second one.

- [ ] **Step 2: Run it and watch it fail**

Run: `bundle exec rspec spec/requests/global_adjustment_spec.rb`
Expected: FAIL — `undefined local variable or method 'admin_team_adjustments_path'`.

- [ ] **Step 3: Add the route**

```ruby
  namespace :admin do
    resources :teams, :only => [] do
      resources :adjustments, :only => [ :new, :create ], :controller => "team_adjustments"
    end
  end
```

Place it beside the existing `admin` namespace block rather than opening a second one. If the
existing block is not written with `namespace`, match whatever form it uses.

- [ ] **Step 4: Write the controller**

Create `app/controllers/admin/team_adjustments_controller.rb`:

```ruby
# -*- encoding : utf-8 -*-
# A global adjustment: one that belongs to the team rather than to any run.
#
# Superadmin-only, unlike its game-scoped counterpart on
# InterventionsController. That controller's ensure_author means "the author,
# any superadmin, or an operator on a gated game" -- all defined RELATIVE TO A
# GAME. A global row has no game, so there is no author for it to admit, and
# it reaches every game the team has ever played. The narrower authority
# matches the wider blast radius.
class Admin::TeamAdjustmentsController < ApplicationController
  include AdminAudit

  before_action :require_authentication!
  before_action :require_superadmin!
  before_action :find_team

  def new
    @amount = nil
    @note   = nil
  end

  def create
    @amount = params[:amount].to_i
    @note   = params[:note].to_s

    return render(:confirm) if params[:confirmed].blank?

    PointTransaction.adjust!(:team => @team, :amount => @amount,
                             :note => @note, :actor => current_user)
    record_admin_action("adjust_points_globally", @team)
    redirect_to admin_teams_path, :notice => t("admin.team_adjustments.done")
  rescue ActiveRecord::RecordInvalid
    render :new, :status => :unprocessable_entity
  end

  private

  def find_team
    @team = Team.find(params[:team_id])
  end
end
```

- [ ] **Step 5: Write the two views**

`app/views/admin/team_adjustments/new.html.erb`:

```erb
<div class="page">
  <h2><%= t("admin.team_adjustments.title", :team => @team.name) %></h2>

  <%= form_with url: admin_team_adjustments_path(:team_id => @team.id), method: :post do %>
    <div class="field">
      <%= label_tag :amount, t("interventions.adjust.amount") %>
      <%= number_field_tag :amount, @amount %>
    </div>
    <div class="field">
      <%= label_tag :note, t("interventions.adjust.note") %>
      <%= text_area_tag :note, @note %>
    </div>
    <%= submit_tag t("interventions.adjust.review"), :class => "btn" %>
  <% end %>
</div>
```

`app/views/admin/team_adjustments/confirm.html.erb`:

```erb
<div class="page">
  <h2><%= t("interventions.adjust.confirm_title") %></h2>

  <p><%= t("admin.team_adjustments.summary", :team => @team.name, :amount => @amount) %></p>

  <%# Operator-authored text: verbatim, never through t(). %>
  <p class="adjust-note"><%= @note %></p>

  <p><%= t("interventions.adjust.balance_change",
           :before => @team.balance, :after => @team.balance + @amount) %></p>

  <%= form_with url: admin_team_adjustments_path(:team_id => @team.id), method: :post do %>
    <%= hidden_field_tag :amount, @amount %>
    <%= hidden_field_tag :note, @note %>
    <%= hidden_field_tag :confirmed, "1" %>
    <%= submit_tag t("interventions.adjust.confirm"), :class => "btn danger-row" %>
  <% end %>

  <%= link_to t("interventions.adjust.cancel"), new_admin_team_adjustment_path(:team_id => @team.id) %>
</div>
```

Reusing `interventions.adjust.*` for the shared labels is deliberate — the two forms ask for the
same two things, and duplicating seven locales' worth of strings so the keys can live under two
namespaces would guarantee they drift.

- [ ] **Step 6: Link it from the admin teams console**

In `app/views/admin/teams/index.html.erb`, in whatever cell holds the per-team actions:

```erb
<%= link_to t("interventions.adjust.button"),
            new_admin_team_adjustment_path(:team_id => team.id), :class => "btn" %>
```

- [ ] **Step 7: Add the locale keys**

Under `admin:`, a new `team_adjustments:` block:

| key | ru | en | uk | be | pl | tr | ka |
|---|---|---|---|---|---|---|---|
| `title` | `Корректировка очков команды «%{team}»` | `Adjust points for %{team}` | `Коригування очок команди «%{team}»` | `Карэкціроўка ачкоў каманды «%{team}»` | `Korekta punktów drużyny „%{team}”` | `«%{team}» adlı takımın puan düzeltmesi` | `გუნდის «%{team}» ქულების კორექტირება` |
| `summary` | `Команде «%{team}» по всем играм: %{amount} очков` | `%{amount} points for %{team}, across all games` | `Команді «%{team}» по всіх іграх: %{amount} очок` | `Камандзе «%{team}» па ўсіх гульнях: %{amount} ачкоў` | `%{amount} punktów dla drużyny „%{team}”, we wszystkich grach` | `«%{team}» adlı takıma tüm oyunlar için %{amount} puan` | `გუნდს «%{team}» ყველა თამაშზე — %{amount} ქულა` |
| `done` | `Очки скорректированы` | `The points were adjusted` | `Очки скориговано` | `Ачкі скарэктаваны` | `Punkty zostały skorygowane` | `Puanlar düzeltildi` | `ქულები კორექტირებულია` |

And the audit label, under `admin:` → `audit:` → `index:` → `action:`:

| locale | `adjust_points_globally` |
|---|---|
| ru | `Скорректировал очки команды по всем играм` |
| en | `Adjusted a team's points across all games` |
| uk | `Скоригував очки команди по всіх іграх` |
| be | `Скарэктаваў ачкі каманды па ўсіх гульнях` |
| pl | `Skorygował punkty drużyny we wszystkich grach` |
| tr | `Bir takımın tüm oyunlardaki puanını düzeltti` |
| ka | `დაკორექტირდა გუნდის ქულები ყველა თამაშზე` |

- [ ] **Step 8: Run the tests**

Run: `bundle exec rspec spec/requests/global_adjustment_spec.rb spec/i18n_spec.rb spec/i18n_audit_actions_spec.rb spec/requests/points_chart_spec.rb`
Expected: PASS. The chart spec is in the command because a global row must still count toward a
team's balance — its aggregation is `group(:team_id).sum(:amount)` with no join, so it should, and
this proves it.

- [ ] **Step 9: Commit**

```bash
git add config/routes.rb app/controllers/admin/team_adjustments_controller.rb \
        app/views/admin/team_adjustments app/views/admin/teams/index.html.erb \
        config/locales spec/requests/global_adjustment_spec.rb
git commit -m "Let a superadmin adjust a team's points across all games"
```

---

## Task 4: A row that belongs to no game

**Files:**
- Modify: `app/controllers/teams_controller.rb` (`#show`, `#game_title_for_viewer`)
- Modify: `app/views/teams/show.html.erb`
- Modify: `config/locales/{ru,en,uk,ka,tr,be,pl}.yml`
- Test: `spec/requests/team_history_spec.rb`

**Interfaces:**
- Consumes: global rows written by Task 3.

`app/controllers/teams_controller.rb:222` currently reads:

```ruby
  def game_title_for_viewer(game)
    return t("teams.show.hidden_game") if game.nil? || !@nameable_game_ids.include?(game.id)

    game.name
  end
```

**A global adjustment would render as "hidden game"** — telling every reader that a game exists
which they are not entitled to see, when there is no game at all. Those are different statements
and the page must not conflate them.

- [ ] **Step 1: Write the failing test**

Add to `spec/requests/team_history_spec.rb`:

```ruby
  # A global adjustment belongs to no game. "There is nothing here to name" and
  # "there is something here you may not see" are different statements, and
  # rendering the second for the first invents a game for the reader.
  it "shows a global adjustment without pretending a hidden game exists" do
    team = create_team(:captain => create_user)
    PointTransaction.adjust!(:team => team, :amount => -25,
                             :note => "Нарушение регламента", :actor => create_user)

    get team_path(team)

    expect(response).to have_http_status(:ok)
    expect(response.body).to include("Нарушение регламента")
    expect(response.body).to include(I18n.t("teams.show.no_game"))
    expect(response.body).not_to include(I18n.t("teams.show.hidden_game"))
  end

  # The note is operator-authored free text on a public page.
  it "escapes markup in a note rather than rendering it" do
    team = create_team(:captain => create_user)
    PointTransaction.adjust!(:team => team, :amount => -1,
                             :note => "<script>alert(1)</script>", :actor => create_user)

    get team_path(team)

    expect(response).to have_http_status(:ok)
    expect(response.body).not_to include("<script>alert(1)</script>")
    expect(response.body).to include("&lt;script&gt;")
  end
```

- [ ] **Step 2: Run it and watch it fail**

Run: `bundle exec rspec spec/requests/team_history_spec.rb`
Expected: FAIL — the first example, because a global row renders the `hidden_game` label.

- [ ] **Step 3: Split the two cases**

```ruby
  # Three states, not two. A row with no game at all is not a row whose game is
  # hidden: saying "hidden game" for a global adjustment tells the reader a
  # game exists that they may not see, which invents one.
  def game_title_for_viewer(game)
    return t("teams.show.no_game")     if game.nil?
    return t("teams.show.hidden_game") unless @nameable_game_ids.include?(game.id)

    game.name
  end
```

- [ ] **Step 4: Show the note in the ledger table**

In `app/views/teams/show.html.erb`, add a column to the transactions table — a header beside
`teams.show.reason`, and in the row, after the reason cell:

```erb
      <%# Operator-authored free text on a public page: rendered verbatim,
          never through t(), and escaped by ERB's default. %>
      <td data-label="<%= t("teams.show.note") %>"><%= row.note %></td>
```

- [ ] **Step 5: Add the locale keys**

| locale | `teams.show.no_game` | `teams.show.note` |
|---|---|---|
| ru | `Вне игры` | `Пояснение` |
| en | `Not tied to a game` | `Explanation` |
| uk | `Поза грою` | `Пояснення` |
| be | `Па-за гульнёй` | `Тлумачэнне` |
| pl | `Poza grą` | `Wyjaśnienie` |
| tr | `Oyun dışı` | `Açıklama` |
| ka | `თამაშის გარეშე` | `განმარტება` |

- [ ] **Step 6: Run the tests**

Run: `bundle exec rspec spec/requests/team_history_spec.rb spec/requests/points_chart_spec.rb spec/i18n_spec.rb`
Expected: PASS, including the **flat query-count guard** already in `team_history_spec.rb` — a
global row must not add a query per row. `@transactions` already preloads `:game`, and preloading a
nil association costs nothing.

- [ ] **Step 7: Commit**

```bash
git add app/controllers/teams_controller.rb app/views/teams/show.html.erb \
        config/locales spec/requests/team_history_spec.rb
git commit -m "Show a row that belongs to no game as such"
```

---

## Gates

The controller runs both full suites, each from its own `bin/rails db:test:prepare` — RSpec and
Cucumber share `db/test.sqlite3`, and rows left by the first run produce phantom failures in the
second.

- After **Task 1**, because it changes a shared index and relaxes two associations every existing
  writer depends on.
- After **Task 4**.

Expected: RSpec 0 failures with only the 6 pre-existing pending examples; Cucumber **238 scenarios
(2 undefined, 236 passed) / 2386 steps**, unchanged — no `.feature` file is touched.

---

## Deliberately not in this plan

- **Editing or deleting an adjustment.** The ledger never reverses; a compensating adjustment is the
  remedy, and both rows stay visible.
- **A level-scoped adjustment.** Spec §8.
- **An amount cap.** Spec A6 — the confirmation page replaces it.
- **Notifying a team that it was adjusted.** Spec §8.
- **D1's missing validations** on `level_completion_points` and `game_completion_points`, which
  still accept negative values from a crafted POST. Raised twice with the repository owner and still
  awaiting a decision; it is not this sub-project's to take.
