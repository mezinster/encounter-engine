# Internal runbook

This file sits in an allowlisted DIRECTORY but is not an allowlisted FILE.
docs/runbooks/ really does hold runbooks that must not be published --
vm-scaling-setup.md names the Azure OIDC identities -- so the allowlist names
runbooks one file at a time rather than globbing the directory.
