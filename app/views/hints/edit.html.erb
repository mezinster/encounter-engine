<p>
  <%= t("hints.edit.heading_prefix") %> "<%= link_to @level.name, game_level_path(@game, @level) %>" <%= t("hints.edit.heading_game_prefix") %> "<%= link_to @game.name, game_path(@game) %>"
</p>

<%= error_messages_for @hint, header: "<h2>#{t("shared.error_header")}</h2>" %>

<%= form_with model: [@game, @level, @hint], url: game_level_hint_path(@game, @level, @hint) do |f| %>

  <%= render "form", f: f %>

  <%= f.submit t("hints.edit.submit"), class: "btn btn--go" %>
  <em><%= link_to t("shared.cancel"), game_level_path(@hint.level.game, @hint.level) %></em>
<% end %>
