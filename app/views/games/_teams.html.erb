<fieldset class="card">
  <legend>
    <%= t("games.teams.legend") %>
  </legend>

  <ul class="game-list">
    <% teams.each do |team| %>
      <li>
        <strong><%= team.name %></strong>
      </li>
    <% end %>
  </ul>
</fieldset>
