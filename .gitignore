.DS_Store
log/*
tmp/*
TAGS
*~
.#*
schema/schema.rb
schema/*_structure.sql
schema/*.sqlite3
schema/*.sqlite
schema/*.db
*.sqlite
*.sqlite3
# sqlite in WAL mode writes db/test.sqlite3-wal and -shm beside the database.
# *.sqlite3 does not match them: the glob requires the name to END in .sqlite3.
# Without this a `git add -A` after any test run stages a binary.
*.sqlite3-*
*.db
src/*
.hgignore
.hg/*
.svn/*
gems/gems/*
gems/specifications/*
gems/doc/*
gems/gems
gems/specifications
gems/doc
out.html
config/database.yml
nbproject
.idea
output
rerun.txt
features/step_definitions
db/development.sqlite
CLAUDE.local.md
.bundle/
# Superpowers scratch (SDD ledgers, brainstorm mockups)
.superpowers/

# git worktrees for parallel hotfixes
.worktrees/
.claude/worktrees/

# Active Storage's :local service (config/storage.yml) writes uploaded game
# files here in development. This repository is public -- never let a
# developer's local uploads land in a commit.
/storage/*
!/storage/.keep
